---
phase: quick/260904-9uy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/cart-context.tsx
  - app/admin/page.tsx
  - __tests__/orders-store.test.ts
autonomous: true
requirements: [CLEAN-DUP-WA]
must_haves:
  truths:
    - "El mensaje de WhatsApp que ve el cliente al confirmar un pedido en /checkout es byte-a-byte identico al de hoy"
    - "El boton 'Reenviar a Delivery' del panel admin sigue abriendo wa.me al numero 51923368745 con nombre, telefono, direccion, GPS (si existe), items con cremas y total"
    - "Existe una sola implementacion del armado del mensaje de pedido en el repo"
  artifacts:
    - path: "lib/cart-context.tsx"
      provides: "buildWhatsAppUrl(order, opts?) unico builder de mensaje de pedido"
      contains: "export function buildWhatsAppUrl"
    - path: "app/admin/page.tsx"
      provides: "Reenvio a delivery delegando en buildWhatsAppUrl"
    - path: "__tests__/orders-store.test.ts"
      provides: "Tests que congelan el texto cliente y cubren las opciones nuevas"
  key_links:
    - from: "app/admin/page.tsx"
      to: "lib/cart-context.tsx"
      via: "import { buildWhatsAppUrl }"
      pattern: "buildWhatsAppUrl"
---

<objective>
`app/admin/page.tsx:108` (`buildDeliveryForwardUrl`) reimplementa el builder de mensaje de WhatsApp que ya existe en `lib/cart-context.tsx:35` (`buildWhatsAppUrl`). Mismo tipo `Order`, mismo proposito, formatos ya divergentes.

Enfoque elegido: **(b) extender `buildWhatsAppUrl` con un parametro opcional**. Es lo mas simple porque las unicas diferencias reales que el reenvio necesita son (1) otro numero destino y (2) una linea de GPS. El resto de las divergencias (encabezado, etiqueta de direccion, items sin precio, prefijo "Cremas:") son cosmeticas e internas: el mensaje de reenvio lo lee Jaime/el motorizado, no el cliente, y converge al formato del cliente sin perder ninguna informacion (de hecho gana los precios por linea).

`buildWhatsAppUrl` sin segundo argumento debe producir EXACTAMENTE el mismo string que hoy — es codigo de produccion user-facing (`app/checkout/page.tsx:242`, respaldo operativo cuando falla el insert en Supabase).

Purpose: eliminar la duplicacion antes de que divergan mas.
Output: un unico builder parametrizado, admin consumiendolo, tests que congelan el output del cliente.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@lib/cart-context.tsx
@app/admin/page.tsx
@__tests__/orders-store.test.ts
@lib/orders-store.ts

<interfaces>
Firma actual (lib/cart-context.tsx:35):
  export function buildWhatsAppUrl(order: Order): string

Consumidores actuales:
  app/checkout/page.tsx:6,242  -> buildWhatsAppUrl(confirmedOrder)
  __tests__/orders-store.test.ts:9

Order (lib/orders-store.ts:12) incluye: id, createdAt, name, phone, delivery, address,
lat?: number, lng?: number, items: {id,name,price,qty,cremas?:string[]}[], total, status.

Constantes relevantes:
  lib/cart-context.tsx:33   WHATSAPP_NUMBER = "51974983862"   (numero del negocio, cliente)
  app/admin/page.tsx:107    DELIVERY_FORWARD_NUMBER = "51923368745" (numero de Jaime, reenvio)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extender buildWhatsAppUrl con opciones (to, includeGps)</name>
  <files>__tests__/orders-store.test.ts, lib/cart-context.tsx</files>
  <behavior>
    Escribir primero estos tests en el describe `buildWhatsAppUrl`:
    - Congelar output cliente: para un Order fijo (`construirOrderLocal(baseInput)`, delivery true),
      `buildWhatsAppUrl(order)` produce el string COMPLETO esperado. Construir el esperado como
      literal explicito en el test (`https://wa.me/51974983862?text=` + encodeURIComponent del mensaje
      con encabezado `Pedido Lobo Burger - #${order.id}`, `Cliente:`, `Telefono:`, `Delivery a: ...`,
      lineas `- 2x Miami Night - S/36`, `Total: S/46`), no derivarlo llamando a la misma funcion.
    - `buildWhatsAppUrl(order, {})` es identico a `buildWhatsAppUrl(order)`.
    - `buildWhatsAppUrl(order, { to: "51923368745" })` cambia solo el host del link; el texto decodificado
      es identico al del caso por defecto.
    - `buildWhatsAppUrl(orderConLatLng, { includeGps: true })` agrega `GPS: https://maps.google.com/?q=<lat>,<lng>`
      al texto; el resto del texto no cambia.
    - `buildWhatsAppUrl(orderSinLatLng, { includeGps: true })` NO contiene "GPS:" ni "undefined".
    - `buildWhatsAppUrl(orderConLatLng)` (sin opts) NO contiene "GPS:".
    - Un Order con `cremas: ["Ají", "Tártara"]` en un item mantiene el formato actual ` (Ají, Tártara)`.
  </behavior>
  <action>
Modificar la firma a `export function buildWhatsAppUrl(order: Order, opts?: { to?: string; includeGps?: boolean }): string`.
Dentro: `const to = opts?.to ?? WHATSAPP_NUMBER;`. Mantener intacta la construccion de `lines`,
`deliveryLine` y `msg` actuales. La linea de GPS se agrega SOLO si `opts?.includeGps` es true Y
`order.lat != null && order.lng != null`, insertandola inmediatamente despues de `deliveryLine`
(mismo salto de linea simple, sin alterar los dobles saltos existentes). Retornar
`https://wa.me/${to}?text=${encodeURIComponent(msg)}`.
Sin opts, la salida debe ser identica a la de hoy: no reordenar campos, no tocar etiquetas,
no cambiar `formatPrice`. No agregar dependencias ni mover la funcion de archivo.
  </action>
  <verify>
    <automated>npx vitest run __tests__/orders-store.test.ts</automated>
  </verify>
  <done>Todos los tests de buildWhatsAppUrl pasan, incluido el que congela el string completo del flujo cliente.</done>
</task>

<task type="auto">
  <name>Task 2: Borrar buildDeliveryForwardUrl y consumir el builder unico en admin</name>
  <files>app/admin/page.tsx</files>
  <action>
Eliminar por completo la funcion `buildDeliveryForwardUrl` (`app/admin/page.tsx:108-120`).
Conservar la constante `DELIVERY_FORWARD_NUMBER = "51923368745"` y el comentario explicativo de
las lineas 103-106 (documenta por que el reenvio es manual via wa.me).
Agregar `import { buildWhatsAppUrl } from "@/lib/cart-context";` junto a los demas imports
(nunca por encima de la directiva `"use client"` de la linea 1 — guardrail #3 de AGENTS.md).
En el `href` del boton "Reenviar a Delivery" (`app/admin/page.tsx:781`) reemplazar
`buildDeliveryForwardUrl(o)` por
`buildWhatsAppUrl(o, { to: DELIVERY_FORWARD_NUMBER, includeGps: true })`.
Si tras el borrado `formatPrice` queda sin usarse en el archivo, quitar su import; si sigue usandose
en otro punto, dejarlo. Verificar con grep antes de tocar el import.
Este cambio va en el mismo commit que la Task 1 (guardrail #1: cero builds rotos).
  </action>
  <verify>
    <automated>grep -c 'buildDeliveryForwardUrl' app/admin/page.tsx | grep -qx '0' && grep -q 'buildWhatsAppUrl(o, { to: DELIVERY_FORWARD_NUMBER, includeGps: true })' app/admin/page.tsx && npx tsc --noEmit && npx vitest run && npm run lint; true</automated>
  </verify>
  <done>No queda ninguna referencia a `buildDeliveryForwardUrl`; `tsc --noEmit` limpio; `vitest run` verde; `npm run lint` con paridad exacta contra la baseline preexistente de 3 errores `react-hooks/set-state-in-effect` (medir la baseline del merge-base antes de comparar — cualquier regla distinta significa regresion).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin panel -> wa.me (URL externa) | Datos del pedido (nombre, telefono, direccion, GPS) salen del sistema hacia un link de WhatsApp |
| datos de pedido -> query string | Contenido controlado parcialmente por el cliente (nombre, direccion) se interpola en una URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9uy-01 | Tampering | `buildWhatsAppUrl` query string | mitigate | El mensaje completo pasa por `encodeURIComponent` una sola vez antes de concatenarse al `?text=`; no interpolar datos crudos en la URL |
| T-9uy-02 | Information disclosure | linea GPS en el mensaje de reenvio | accept | Lat/lng del cliente ya se enviaban en el flujo actual de reenvio a delivery; el destinatario (motorizado) los necesita para entregar |
| T-9uy-03 | Repudiation | mensaje cliente alterado por refactor | mitigate | Test que congela el string completo del flujo cliente (Task 1) antes de tocar la implementacion |
| T-9uy-SC | Tampering | npm installs | mitigate | No se instalan paquetes en esta tarea |
</threat_model>

<verification>
1. `npx tsc --noEmit && npx vitest run && npm run lint` — pegar salida real, lint con paridad de baseline.
2. `grep -rn 'buildDeliveryForwardUrl' app components lib __tests__` devuelve vacio.
3. `grep -rn 'wa.me' app components lib` muestra una sola construccion de URL (dentro de `lib/cart-context.tsx`).
</verification>

<success_criteria>
- Una sola implementacion del mensaje de pedido en todo el repo.
- Output de `buildWhatsAppUrl(order)` sin opciones identico byte a byte al de produccion actual, con test que lo congela.
- Boton "Reenviar a Delivery" apunta a `wa.me/51923368745` e incluye GPS cuando el pedido tiene lat/lng.
- Gate pre-vuelo de AGENTS.md ejecutado con salida real pegada.
</success_criteria>

<output>
Crear `.planning/quick/260904-9uy-fix-builddeliveryforwardurl-en-app-admin/260904-9uy-SUMMARY.md` al terminar.
</output>
