---
phase: quick-260904-83o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/order-transition.ts
  - __tests__/order-transition.test.ts
  - app/admin/page.tsx
autonomous: true
requirements: [QUICK-83o]
must_haves:
  truths:
    - "Un PATCH que devuelve HTTP 500 se trata como fallo, no como exito silencioso"
    - "Si la persistencia de en_preparacion falla, el pedido vuelve a estado pendiente en el KDS"
    - "Si la persistencia falla, el id sale de knownOrderIdsRef y el siguiente refresh() lo vuelve a detectar como entrante (reintento)"
    - "El staff ve un banner en la pestana Pedidos cuando un pedido no pudo persistir su transicion"
    - "Un pedido cuya transicion persistio correctamente no reimprime ni vuelve a sonar"
  artifacts:
    - path: "lib/order-transition.ts"
      provides: "Helper puro y testeable de persistencia de cambio de estado"
      exports: ["persistOrderTransition"]
    - path: "__tests__/order-transition.test.ts"
      provides: "Cobertura de ok/HTTP-error/network-error"
    - path: "app/admin/page.tsx"
      provides: "Wiring del helper en onPrinted + rollback + senal visible"
  key_links:
    - from: "app/admin/page.tsx"
      to: "lib/order-transition.ts"
      via: "import { persistOrderTransition }"
      pattern: "persistOrderTransition"
    - from: "onPrinted callback"
      to: "knownOrderIdsRef"
      via: "delete del id ante fallo de persistencia"
      pattern: "knownOrderIdsRef.current.delete"
---

<objective>
Cerrar el agujero por el que un pedido puede quedar "pendiente para siempre": hoy en
`app/admin/page.tsx` el `onPrinted` de `scheduleAutoPrint` hace un PATCH fire-and-forget
con `.catch(() => {})` (linea ~530-534) y el id ya fue agregado a `knownOrderIdsRef`
en la deteccion (linea ~513). Si el PATCH falla — o devuelve 500, que hoy ni siquiera se
detecta porque `fetch` no rechaza en HTTP error — la UI muestra `en_preparacion` optimista,
la DB sigue en `pendiente`, y el pedido nunca vuelve a entrar en `incomingOrders`.

Purpose: que ningun pedido pagado se pierda entre la impresion y la base de datos, y que
si se pierde, la cocina lo vea.
Output: helper puro `lib/order-transition.ts` con tests, y wiring en el panel con rollback,
reintento y banner visible.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@lib/auto-print.ts
@app/admin/page.tsx
@app/api/admin/pedidos/route.ts
@__tests__/auto-print.test.ts

<interfaces>
De `lib/auto-print.ts` (ya existe, NO se modifica):
```ts
export interface ScheduleAutoPrintOptions<T> {
  orders: T[];
  onStage: (order: T) => void;
  onPrint: (order: T) => void;
  onPrinted: (order: T) => void;
  intervalMs?: number;
  printDelayMs?: number;
}
export function scheduleAutoPrint<T>(opts: ScheduleAutoPrintOptions<T>): ReturnType<typeof setTimeout>[];
```
`onPrinted` es sincrono en su firma: cualquier trabajo async dentro se dispara y no se
espera. Eso es correcto — el helper de persistencia maneja su propio ciclo.

De `app/api/admin/pedidos/route.ts`: `PATCH` acepta `{ codigo: string, estado: string }`,
devuelve `200 {ok:true}`, `400 {error}` con datos invalidos, `500 {error}` si Supabase falla.
El 500 es exactamente el caso que hoy pasa desapercibido.

De `lib/orders-store.ts`: `type OrderStatus = "pendiente" | "en_preparacion" | "listo" | "entregado"`.
</interfaces>

Convenciones que aplican (de CLAUDE.md):
- Imports internos siempre con alias `@/...`, nunca relativos.
- Constantes de modulo en SCREAMING_SNAKE_CASE, funciones camelCase, nombres de dominio en espanol.
- Comillas dobles, punto y coma, indentacion de 2 espacios. Sin Prettier.
- Comentarios solo donde explican un *por que* no obvio (patron del repo).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper puro persistOrderTransition + tests</name>
  <files>lib/order-transition.ts, __tests__/order-transition.test.ts</files>
  <behavior>
    - `persistOrderTransition({ codigo: "LB-1", estado: "en_preparacion" })` con fetch que resuelve `{ok:true, status:200}` -> devuelve `{ ok: true }`.
    - Con fetch que resuelve `{ok:false, status:500}` -> devuelve `{ ok: false, reason: "http_500" }` (no lanza).
    - Con fetch que rechaza (`new TypeError("Failed to fetch")`) -> devuelve `{ ok: false, reason: "network" }` (no lanza).
    - Envia `method: "PATCH"`, header `Content-Type: application/json` y body JSON exacto `{"codigo":"LB-1","estado":"en_preparacion"}` a `"/api/admin/pedidos"`.
  </behavior>
  <action>
Crear `lib/order-transition.ts` siguiendo el mismo contrato de pureza que `lib/auto-print.ts`:
sin `"use client"`, sin DOM, sin React, para poder testearlo con un `fetch` inyectado.

Exportar:
- `type TransitionResult = { ok: true } | { ok: false; reason: string }`
- `interface PersistOrderTransitionOptions { codigo: string; estado: string; fetchImpl?: typeof fetch }`
- `async function persistOrderTransition(opts: PersistOrderTransitionOptions): Promise<TransitionResult>`

Comportamiento: llama a `fetchImpl ?? fetch` sobre `"/api/admin/pedidos"` con `method: "PATCH"`,
header `Content-Type: application/json`, body `JSON.stringify({ codigo, estado })`. Envuelve la
llamada en try/catch. La funcion NUNCA lanza: devuelve `{ ok:false, reason: "network" }` si el
fetch rechaza y `{ ok:false, reason: \`http_${res.status}\` }` si `res.ok` es false. Solo devuelve
`{ ok:true }` cuando `res.ok` es true.

Comentario de cabecera corto explicando el porque: `fetch` no rechaza ante HTTP 500, asi que un
`.catch()` suelto deja pasar el fallo real de Supabase como si fuera exito.

Crear `__tests__/order-transition.test.ts` con vitest (mismo estilo que `__tests__/auto-print.test.ts`:
`describe`/`it`/`expect`, `vi.fn()` para el `fetchImpl` inyectado, sin fake timers porque no hay
temporizadores aca). Cubrir los cuatro casos de `<behavior>`, incluyendo la asercion del body exacto
leyendo el argumento con el que se llamo el mock.

NO tocar `lib/auto-print.ts` ni `app/admin/page.tsx` en esta tarea: el archivo nuevo no tiene
consumidores todavia, asi que el arbol compila igual (guardrail #1).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run __tests__/order-transition.test.ts</automated>
  </verify>
  <done>`lib/order-transition.ts` existe y exporta `persistOrderTransition`; los 4 tests de `__tests__/order-transition.test.ts` pasan; `npx tsc --noEmit` sin errores.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring en /admin — rollback, reintento y senal visible</name>
  <files>app/admin/page.tsx</files>
  <action>
En `app/admin/page.tsx`, importar `persistOrderTransition` desde `@/lib/order-transition`
(alias `@/`, junto a los imports existentes de `@/lib/...`).

1) **Estado de fallos visibles.** Agregar `const [failedTransitions, setFailedTransitions] = useState<string[]>([])`
junto a los demas `useState` del componente `AdminPage`. Guarda los ids (`Order["id"]`) de pedidos
cuya transicion a `en_preparacion` no se pudo persistir.

2) **Reescribir el callback `onPrinted`** dentro de `refresh()` (hoy lineas ~528-535). Debe:
   - Mantener la actualizacion optimista `setOrders(list => list.map(...))` a `en_preparacion`.
   - Llamar a `persistOrderTransition({ codigo: toPrint.id, estado: "en_preparacion" })` y manejar
     el resultado con `.then(...)` (el callback de `scheduleAutoPrint` es sincrono, no se puede
     `await` — no cambiar la firma de `lib/auto-print.ts`).
   - Si `result.ok === true`: no hacer nada mas; el id ya esta en `knownOrderIdsRef` y la transicion
     quedo persistida. Ademas, quitar el id de `failedTransitions` si estaba (caso reintento exitoso).
   - Si `result.ok === false`:
     a. `console.error` con el codigo del pedido y `result.reason` (senal minima obligatoria).
     b. Revertir el estado local del pedido a `"pendiente"` con `setOrders(...)`, para que el KDS no
        mienta y el timbre persistente vuelva a contarlo.
     c. `knownOrderIdsRef.current.delete(toPrint.id)` — este es el nucleo del fix: el id deja de estar
        "resuelto", asi que el siguiente `refresh()` (poll de 10s) lo vuelve a incluir en
        `incomingOrders` y reintenta impresion + PATCH. Sin esto el pedido queda huerfano para siempre.
     d. `setFailedTransitions(prev => prev.includes(toPrint.id) ? prev : [...prev, toPrint.id])`.

   Actualizar el comentario que ya existe encima del bloque de auto-impresion para reflejar que el id
   solo se considera resuelto tras persistencia exitosa.

   Mantener el `knownOrderIdsRef.current.add(o.id)` de la deteccion (linea ~513) tal cual: sigue siendo
   necesario para que el poll de 10s no dispare una segunda impresion mientras el PATCH esta en vuelo;
   el `delete` ante fallo es lo que habilita el reintento.

3) **Limpieza en `refresh()`.** Despues de construir `mapped`, purgar de `failedTransitions` los ids
cuyo pedido en `mapped` ya tenga `estado !== "pendiente"` (la DB confirmo la transicion por otra via,
p.ej. el boton manual "A LA PLANCHA"). Usar `setFailedTransitions(prev => prev.filter(...))` y evitar
crear un array nuevo si no cambio nada, para no disparar renders innecesarios en una pantalla que vive
todo el turno.

4) **Banner visible en la pestana Pedidos.** Dentro del bloque `{tab === "pedidos" && (...)}`, arriba de
la barra de filtros, renderizar condicionalmente (`failedTransitions.length > 0`) un aviso con el estilo
de error ya usado en el archivo (`background: "rgba(231,76,60,0.12)"`, `color: "#e74c3c"`,
`border: "1px solid rgba(231,76,60,0.3)"`, `rounded-xl`, texto `text-xs font-semibold`), con el icono
`X` de lucide (ya importado). Texto: cantidad de pedidos afectados y sus codigos, mas
"no se pudo confirmar en la base de datos — se reintentara automaticamente". Incluir un boton chico
que llame a `refresh` para forzar el reintento manual sin esperar los 10s.

No agregar dependencias, no tocar `app/api/admin/pedidos/route.ts`, no cambiar el intervalo de polling
ni `lib/auto-print.ts`. `handleStatus` y `handleUndo` quedan como estan (fuera de alcance).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <done>`app/admin/page.tsx` importa y usa `persistOrderTransition`; ante fallo hace `console.error`, revierte a `pendiente`, ejecuta `knownOrderIdsRef.current.delete(id)` y agrega el id a `failedTransitions`; el banner de error se renderiza en la pestana Pedidos; `npx tsc --noEmit` limpio; toda la suite de vitest verde; `npm run lint` en paridad con la baseline (solo los 3 `react-hooks/set-state-in-effect` preexistentes en `app/puntos/page.tsx` y `lib/cart-context.tsx`, ninguna regla nueva).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador /admin -> /api/admin/pedidos | Ya protegido por Basic Auth en `proxy.ts`; este cambio no lo altera |
| /api/admin/pedidos -> Supabase | Frontera de fallo (no de confianza): es la que hoy se traga silenciosamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83o-01 | Repudiation | `onPrinted` en `app/admin/page.tsx` | mitigate | El fallo de persistencia deja de ser silencioso: `console.error` + banner en UI + entrada en `failedTransitions` |
| T-83o-02 | Denial of Service | reintento por poll de 10s | accept | El reintento esta acotado al poll existente (10s) y solo reencola pedidos que la DB sigue reportando `pendiente`; sin backoff exponencial ni loop nuevo |
| T-83o-03 | Tampering | payload del PATCH | accept | El servidor ya valida `estado` contra la lista `ESTADOS` y `codigo` como string; la ruta esta detras de Basic Auth |
| T-83o-SC | Tampering | npm/pip/cargo installs | mitigate | No se instala ningun paquete en este quick task; nada que auditar |
</threat_model>

<verification>
1. `npx tsc --noEmit && npx vitest run && npm run lint` — gate pre-vuelo del AGENTS.md, pegando salida real.
2. `grep -n "knownOrderIdsRef.current.delete" app/admin/page.tsx` devuelve al menos una coincidencia dentro del manejo de fallo.
3. `grep -vn '^//' app/admin/page.tsx | grep -c "catch(() => {})"` sobre el bloque de `onPrinted`: el `.catch` vacio del PATCH de transicion ya no existe.
4. `grep -rn "persistOrderTransition" app lib __tests__` muestra definicion, test y consumidor.
</verification>

<success_criteria>
- Un PATCH que responde 500 se detecta como fallo (antes pasaba como exito).
- Ante fallo: el pedido vuelve a `pendiente` en el KDS, sale de `knownOrderIdsRef` y el siguiente poll lo reintenta.
- Ante fallo hay `console.error` y un banner rojo visible en la pestana Pedidos con los codigos afectados.
- Ante exito el comportamiento actual no cambia: sin reimpresion, sin timbre duplicado, sin banner.
- Build verde y suite de vitest completa en verde; lint en paridad con la baseline de 3 errores preexistentes.
</success_criteria>

<output>
Crear `.planning/quick/260904-83o-fix-patch-de-transicion-a-en-preparacion/260904-83o-SUMMARY.md` al terminar.
</output>
