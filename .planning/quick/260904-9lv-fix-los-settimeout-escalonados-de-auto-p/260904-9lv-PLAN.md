---
phase: quick-260904-9lv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/auto-print.ts
  - __tests__/auto-print.test.ts
  - app/admin/page.tsx
autonomous: true
requirements: [QT-9LV-01]
must_haves:
  truths:
    - "Al desmontarse /admin (o cambiar de pestaña que desmonte el feed), ningún timer pendiente dispara window.print() ni marca en_preparacion"
    - "Cuando llega un batch nuevo de pedidos mientras el batch anterior sigue escalonando, los timers del batch anterior se cancelan y no se solapan dos secuencias de impresión"
    - "Ningún pedido cancelado a mitad del escalonado se pierde: sus ids se liberan de knownOrderIdsRef y el siguiente refresh los vuelve a detectar e imprimir"
    - "printTimersRef no acumula grupos de timers ya consumidos durante un turno completo de cocina"
    - "El escalonado de 1.5s y el orden print → marcar en_preparacion siguen intactos"
  artifacts:
    - path: "lib/auto-print.ts"
      provides: "scheduleAutoPrint devuelve un handle cancelable que reporta los pedidos no impresos"
      contains: "cancel"
    - path: "__tests__/auto-print.test.ts"
      provides: "Cobertura de cancelación: timers limpiados + pedidos pendientes reportados"
    - path: "app/admin/page.tsx"
      provides: "Bookkeeping de timers en ref con cancelación en nuevo batch y en cleanup del efecto"
  key_links:
    - from: "app/admin/page.tsx"
      to: "lib/auto-print.ts"
      via: "handle devuelto por scheduleAutoPrint guardado en printTimersRef"
      pattern: "printTimersRef"
    - from: "app/admin/page.tsx"
      to: "knownOrderIdsRef"
      via: "liberación de ids no impresos al cancelar un batch"
      pattern: "knownOrderIdsRef.current.delete"
---

<objective>
Cerrar la fuga de timers del auto-print del KDS: los `setTimeout` escalonados que
programa `scheduleAutoPrint` se cancelan de forma determinista cuando /admin se
desmonta y cuando llega un batch nuevo, sin perder ningún pedido.

Purpose: guardrail #5 de AGENTS.md — /admin queda abierta todo el turno de cocina;
timers sueltos disparan `window.print()` y `setPrintingOrder` con estado stale, lo
que puede imprimir la comanda equivocada (dos secuencias solapadas compiten por el
mismo `printingOrder`).
Output: `scheduleAutoPrint` con handle cancelable, tests de cancelación, y
bookkeeping completo en `app/admin/page.tsx`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@lib/auto-print.ts
@__tests__/auto-print.test.ts
@app/admin/page.tsx

<state_actual>
`printTimersRef` ya existe (app/admin/page.tsx:469) y el cleanup del efecto de
polling ya limpia los grupos (líneas 571-577). Lo que falta es:
1. Cancelar el batch anterior cuando llega uno nuevo (líneas 526-557): hoy se
   hace `printTimersRef.current.push(timers)` y los grupos se acumulan sin podarse
   nunca durante todo el turno.
2. No perder pedidos al cancelar: si un batch se cancela a mitad, sus pedidos no
   impresos deben volver a considerarse entrantes.
</state_actual>

<interfaces>
Contrato actual de lib/auto-print.ts:

```
export interface ScheduleAutoPrintOptions<T> {
  orders: T[];
  onStage: (order: T) => void;
  onPrint: (order: T) => void;
  onPrinted: (order: T) => void;
  intervalMs?: number;   // default 1500
  printDelayMs?: number; // default 300
}
export function scheduleAutoPrint<T>(opts: ScheduleAutoPrintOptions<T>): ReturnType<typeof setTimeout>[]
```

Idioma de recuperación ya presente en app/admin/page.tsx (línea 551) cuando el
PATCH falla: `knownOrderIdsRef.current.delete(id)` libera el id para que el
siguiente `refresh()` lo vuelva a detectar como entrante.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scheduleAutoPrint devuelve un handle cancelable</name>
  <files>lib/auto-print.ts, __tests__/auto-print.test.ts</files>
  <behavior>
    - `scheduleAutoPrint(...)` devuelve `AutoPrintHandle<T>` con `timers` (el array mutable actual) y `cancel(): T[]`.
    - `cancel()` limpia todos los timers programados (incluidos los `printTimer` internos ya encolados) y devuelve los pedidos que aún NO llegaron a `onPrint`.
    - Tras `cancel()` no se dispara ningún `onStage`/`onPrint`/`onPrinted` adicional aunque se avancen los timers.
    - `cancel()` llamado después de que la cola terminó devuelve `[]`.
    - `cancel()` es idempotente: dos llamadas seguidas no lanzan y la segunda devuelve `[]`.
    - Se preservan los 3 tests existentes de orden/escalonado/fallo de impresora (el escalonado sigue siendo 1500ms y print→printed sigue siendo el orden).
  </behavior>
  <action>
    Refactorizar `lib/auto-print.ts` para que devuelva un handle en vez de un array pelado.
    Exportar la interfaz `AutoPrintHandle<T>` con los campos `timers` (mismo array mutable
    que hoy, para no romper el patrón de referencia) y `cancel`.

    Internamente mantener un `Set<T>` (o array) de pedidos pendientes inicializado con
    `orders`; sacar el pedido del set justo después de que `onPrint(order)` retorna sin
    lanzar — es decir, en el mismo punto donde hoy se llama `onPrinted`, y solo si
    `onPrint` no lanzó (si la impresora falla, ese pedido cuenta como NO impreso y debe
    volver en el resultado de `cancel()`). Mantener intacto el `try/catch` que evita que
    un fallo de impresión rompa la cola.

    `cancel()` hace `timers.forEach(clearTimeout)`, vacía el array de timers, marca un
    flag `cancelled` para que cualquier callback ya en vuelo no re-encole, y devuelve la
    lista de pendientes (vaciándola luego para la idempotencia).

    No usar `"use client"`, DOM ni React en este archivo: es un módulo puro compartido,
    mismo contrato que `lib/menu.ts`.

    Actualizar `__tests__/auto-print.test.ts`: el test existente "cancela timers
    pendientes con clearTimeout" pasa a usar `handle.cancel()`. Agregar los casos nuevos
    de la sección behavior (pendientes devueltos a mitad de escalonado, cola terminada
    devuelve `[]`, idempotencia). No tocar los 3 tests de orden/escalonado/fallo.
  </action>
  <verify>
    <automated>npx vitest run __tests__/auto-print.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Todos los tests de auto-print pasan (los 3 previos + los nuevos de cancelación) y `tsc --noEmit` está limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Bookkeeping y cancelación de timers en /admin</name>
  <files>app/admin/page.tsx</files>
  <action>
    Cambiar el tipo de `printTimersRef` (línea ~469) de `ReturnType<typeof setTimeout>[][]`
    a un array de handles: `useRef<AutoPrintHandle<Order>[]>([])`. Renombrarlo a
    `printHandlesRef` si eso deja el código más claro; actualizar el comentario de las
    líneas 466-468 para que describa la nueva política (cancelar batch anterior + liberar
    ids no impresos), no el detalle del array mutable.

    En el bloque de auto-impresión (líneas ~537-557), ANTES de programar el batch nuevo:
    cancelar todos los handles acumulados, recolectar los pedidos que devuelven sus
    `cancel()` y hacer `knownOrderIdsRef.current.delete(o.id)` por cada uno, para que el
    siguiente `refresh()` (polling de 10s) los vuelva a detectar como entrantes y los
    reimprima. Esto evita a la vez el solapamiento de dos secuencias compitiendo por
    `printingOrder` y la pérdida de comandas. Después vaciar el array y pushear el handle
    nuevo — así el ref nunca acumula más de un grupo y no hay fuga en un turno largo.

    Ojo con el orden respecto de la línea ~524 (`incomingOrders.forEach(add)`): la
    liberación de ids del batch cancelado no debe borrar ids del batch entrante. Si hace
    falta, filtrar los ids liberados contra `incomingOrders`.

    En el cleanup del efecto de polling (líneas ~571-577): reemplazar el doble `forEach`
    por `handle.cancel()` sobre cada handle y vaciar el ref. En desmontaje NO se liberan
    ids en `knownOrderIdsRef` (el componente ya no existe; el remontaje hace un
    `initialLoadRef` fresco).

    No tocar `intervalMs`/`printDelayMs` ni el orden print → `en_preparacion` → PATCH
    fijado en 260904-7xt. No tocar el `undoTimerRef` ni el `setTimeout` del toast.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run lint 2>&1 | grep -v 'react-hooks/set-state-in-effect' | grep -c 'error' </automated>
  </verify>
  <done>
    `tsc --noEmit` limpio, toda la suite de vitest verde, y `npm run lint` en paridad con
    la baseline preexistente (solo los 3 errores `react-hooks/set-state-in-effect` de
    `app/puntos/page.tsx` y `lib/cart-context.tsx`; cero reglas nuevas). Medir la baseline
    del merge-base antes de comparar, no asumirla.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador /admin → API `/api/admin/pedidos` | Transiciones de estado de pedidos; ya gateado por Basic Auth en `proxy.ts` |
| timers del navegador → estado de React | Un callback stale puede escribir estado de un componente/pedido que ya no es el actual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9LV-01 | Tampering | `setPrintingOrder` disparado por timer stale en `app/admin/page.tsx` | mitigate | Cancelar el batch anterior antes de programar uno nuevo; una sola secuencia activa a la vez |
| T-9LV-02 | Denial of Service | acumulación de grupos de timers en `printTimersRef` durante un turno completo | mitigate | Vaciar el ref en cada batch nuevo y en el cleanup del efecto |
| T-9LV-03 | Repudiation | pedido pagado que se cancela a mitad del escalonado y nunca llega a cocina | mitigate | `cancel()` devuelve los no impresos; sus ids se liberan de `knownOrderIdsRef` y el polling los reimprime |
| T-9LV-SC | Tampering | instalación de paquetes | accept | Esta tarea no agrega dependencias |
</threat_model>

<verification>
1. `npx tsc --noEmit` — cero errores.
2. `npx vitest run` — suite completa verde, incluidos los casos nuevos de cancelación.
3. `npm run lint` — paridad con la baseline de 3 errores `react-hooks/set-state-in-effect`.
4. `grep -n "printTimersRef\|printHandlesRef" app/admin/page.tsx` — el ref se referencia en el bloque de auto-print Y en el cleanup del efecto de polling.
</verification>

<success_criteria>
- Ningún `setTimeout` de auto-print sobrevive al desmontaje de /admin ni a la llegada de un batch nuevo.
- Los pedidos de un batch cancelado que no alcanzaron a imprimirse vuelven a la cola en el siguiente refresh (cero comandas perdidas).
- Escalonado de 1.5s y orden print → marcar `en_preparacion` sin cambios.
- Gate de AGENTS.md #2 corrido con salida real pegada en el SUMMARY.
- Sin dependencias nuevas.
</success_criteria>

<output>
Crear `.planning/quick/260904-9lv-fix-los-settimeout-escalonados-de-auto-p/260904-9lv-SUMMARY.md` al terminar.
</output>
