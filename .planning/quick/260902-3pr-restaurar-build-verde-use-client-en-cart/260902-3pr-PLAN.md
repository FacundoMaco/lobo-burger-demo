---
phase: quick-260902-3pr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/menu.ts
  - lib/menu-data.ts
  - __tests__/menu.test.ts
  - lib/cart-context.tsx
  - app/admin/page.tsx
autonomous: true
requirements: [BUILD-VERDE]

must_haves:
  truths:
    - "`npx tsc --noEmit` termina sin errores"
    - "`npx vitest run` pasa al 100%, incluido el recuperado `__tests__/menu.test.ts`"
    - "`lib/cart-context.tsx` tiene `\"use client\";` como primera sentencia del archivo"
    - "La carta en `/` renderiza los 17 productos bajo las 5 categorias viejas (Combos, Burgers, Pollo, Complementos, Bebidas), ninguna vacia"
    - "`app/admin/page.tsx` vuelve a la version de HEAD: no llama a `/api/admin/puntos` ni `/api/admin/canjear`"
    - "`npm run lint` ya no reporta `@typescript-eslint/no-unused-expressions`, y no aparece ninguna regla nueva fuera de `react-hooks/set-state-in-effect`"
  artifacts:
    - path: "lib/menu.ts"
      provides: "MENU_ITEMS (17 filas), getMenuItem, BY_ID, CATEGORIES viejas, tipo MenuItem con agotado"
      contains: "export function getMenuItem"
    - path: "__tests__/menu.test.ts"
      provides: "Smoke test de getMenuItem y unicidad de ids"
      contains: "getMenuItem(999)"
    - path: "lib/menu-data.ts"
      provides: "MENU_CATEGORIES (orden canonico de las categorias reales de Supabase)"
      contains: "MENU_CATEGORIES"
  key_links:
    - from: "app/page.tsx"
      to: "lib/menu.ts"
      via: "import { MENU_ITEMS as menuItems, CATEGORIES as categories, type MenuItem }"
      pattern: "MENU_ITEMS as menuItems"
    - from: "app/api/charge/route.ts"
      to: "lib/menu.ts"
      via: "getMenuItem(linea.id) para recalcular el total server-side"
      pattern: "getMenuItem\\(linea\\.id\\)"
    - from: "app/api/culqi/order/route.ts"
      to: "lib/menu.ts"
      via: "getMenuItem(linea.id) para recalcular el total server-side"
      pattern: "getMenuItem\\(linea\\.id\\)"
    - from: "lib/menu-data.ts"
      to: "lib/menu.ts"
      via: "rowToMenuItem devuelve MenuItem, que debe seguir teniendo agotado"
      pattern: "agotado: Boolean\\(row\\.agotado\\)"
---

<objective>
Restaurar el build verde de la rama `pricing-desarrollo-3-cuotas`. Hoy el arbol esta roto en dos
frentes independientes: (a) `lib/menu.ts` borro `MENU_ITEMS`/`getMenuItem`/`CATEGORIES` viejas que
tres archivos siguen importando (TS2724 x3 + TS7006 x2, 23 tests en rojo), y (b)
`lib/cart-context.tsx` tiene un import por encima de `"use client";`, lo que hace que Next 16 tire
`The "use client" directive must be placed before other expressions` y que TODAS las rutas devuelvan
500 (porque `app/layout.tsx` monta `CartProvider`), incluido `/admin`.

Purpose: es un repo que cobra soles reales en produccion. El arbol tiene que volver a compilar antes
de que el plan 02-02 migre los route handlers a Supabase.
Output: `npx tsc --noEmit` verde, `npx vitest run` verde, `npm run lint` sin regresiones nuevas, y la
carta renderizando sus 17 productos.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md

Estado actual verificado empiricamente por el orquestador — NO hace falta re-diagnosticar, solo
arreglar:

- `lib/menu.ts` (HEAD): solo exporta `type MenuItem` (con `agotado: boolean`) y unas `CATEGORIES`
  nuevas `["Enchiladas", "Broaster", "Salchipapas / Power Plates", "Combos xtremos", "Bebidas",
  "Hamburguesas"] as const`. Ya no exporta `MENU_ITEMS`, `getMenuItem` ni `BY_ID`.
- Consumidores rotos (los unicos tres en todo el repo, ya greppeados):
  - `app/page.tsx:12` — `import { MENU_ITEMS as menuItems, CATEGORIES as categories, type MenuItem } from "@/lib/menu";`
    (usa `menuItems.filter` en la linea 165 y `categories.map` en la 199)
  - `app/api/charge/route.ts:8` y `:107` — `getMenuItem(linea.id)`
  - `app/api/culqi/order/route.ts:9` y `:60` — `getMenuItem(linea.id)`
- `lib/menu-data.ts` es `server-only`, mapea filas de Supabase a `MenuItem` y usa `row.agotado`.
  Por eso el campo `agotado` del tipo NO se puede quitar.
- `git status` solo muestra `app/admin/page.tsx` modificado (mas archivos de `.planning/`). El resto
  ya esta commiteado.
- Baseline de lint medida hoy: `10 problems (8 errors, 2 warnings)`, distribuidos en
  `__tests__/helpers/supabase-mock.ts`, `__tests__/menu-data.test.ts`, `app/admin/page.tsx`,
  `app/puntos/page.tsx` y `lib/cart-context.tsx`. Uno de esos 2 warnings ES el
  `@typescript-eslint/no-unused-expressions` de `lib/cart-context.tsx:2` (el sintoma de la directiva
  mal ubicada) y debe desaparecer. Los `react-hooks/set-state-in-effect` son preexistentes y quedan
  fuera de alcance.
</context>

<no_toques>
Trabajo valido del plan 02-02, esperando. NO modificar (unica excepcion: agregar la constante nueva
a `lib/menu-data.ts`):

- `lib/menu-data.ts` — solo se le AGREGA `MENU_CATEGORIES`, nada mas se toca
- `supabase/migrations/20260901000000_menu_items.sql`
- `__tests__/menu-data.test.ts`
- `__tests__/helpers/supabase-mock.ts`
- `package.json` / `package-lock.json` (la dependencia `server-only` se queda)
- `.context/kds-admin-page.tsx.bak` (el backup del KDS ya existe, NO regenerarlo)
</no_toques>

<tasks>

<task type="auto">
  <name>Task 1: Restaurar MENU_ITEMS/getMenuItem en lib/menu.ts, mover las categorias nuevas a menu-data.ts y recuperar el test</name>
  <files>lib/menu.ts, lib/menu-data.ts, __tests__/menu.test.ts</files>
  <action>
Los tres cambios van en UN SOLO commit: el test recuperado importa `getMenuItem`, asi que separarlo
dejaria un commit con vitest en rojo.

Paso 1 — antes de sobreescribir `lib/menu.ts`, copiar a mano el bloque de `CATEGORIES` actual (las
seis categorias reales: Enchiladas, Broaster, "Salchipapas / Power Plates", "Combos xtremos",
Bebidas, Hamburguesas) porque se va a perder en el checkout del paso 2. Podes leerlo con
`git show HEAD:lib/menu.ts` en cualquier momento.

Paso 2 — restaurar el archivo previo completo:
`git checkout 875ae419bb7e9c3900c0980faa1c717de677a630 -- lib/menu.ts __tests__/menu.test.ts`
Eso devuelve `MENU_ITEMS` con los 17 productos, `BY_ID`, `getMenuItem`, y las `CATEGORIES` viejas
`["Combos", "Burgers", "Pollo", "Complementos", "Bebidas"]` (sin `as const`, tipo `string[]`, que es
lo que `app/page.tsx:199` espera para que `categories.map(cat => ...)` no de TS7006). Tambien vuelve
el smoke test que verifica `getMenuItem(1) === Miami Night a S/18`, `getMenuItem(999) === undefined`
y unicidad de ids.

Paso 3 — reintroducir `agotado` en `lib/menu.ts`. El campo NO se puede perder porque
`lib/menu-data.ts` construye objetos `MenuItem` con `agotado: Boolean(row.agotado)`:
  - agregar `agotado: boolean;` al final del `type MenuItem`
  - agregar `agotado: false` como ultima propiedad de las 17 filas de `MENU_ITEMS`
El literal `MENU_ITEMS` usa alineacion en columnas (convencion documentada del repo, ver
CONVENTIONS): rellena con espacios los valores de `image:` para que todos los `agotado: false`
arranquen en la misma columna. En la fila `id: 13` sacá el cast redundante `as string | null` del
valor de `image` — el array ya esta anotado como `MenuItem[]`, el cast no aporta nada y descuadra la
tabla. `npx tsc --noEmit` es el guardrail de que sacarlo es seguro.

Paso 4 — el comentario de cabecera de `lib/menu.ts`: queda el previo (el que explica que el archivo
se importa desde cliente y servidor y que el precio que cobra `/api/charge` sale de aca), porque
vuelve a describir el comportamiento real. Agregale una linea aclarando que `agotado` forma parte del
contrato compartido con `lib/menu-data.ts` y que hoy siempre es `false` en esta fuente estatica.

Paso 5 — mover las categorias nuevas a `lib/menu-data.ts` (que ya es `server-only`), exportadas como
`MENU_CATEGORIES` para que no colisionen con el `CATEGORIES` de `lib/menu.ts`. Van cerca del tope del
archivo, despues de los imports, con un comentario corto: orden canonico de las categorias reales de
la tabla `menu_items`, consumido por los route handlers cuando el plan 02-02 los migre. Mantene el
`as const`. No toques nada mas de `lib/menu-data.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run</automated>
    <automated>grep -c "agotado: false" lib/menu.ts   # debe imprimir 17</automated>
    <automated>grep -q "MENU_CATEGORIES" lib/menu-data.ts &amp;&amp; grep -q "export function getMenuItem" lib/menu.ts</automated>
  </verify>
  <done>
`npx tsc --noEmit` sale limpio (se fueron los 3 TS2724 y los 2 TS7006 de `app/page.tsx`).
`npx vitest run` pasa entero, incluidos los 3 casos de `__tests__/menu.test.ts` y los
`__tests__/menu-data.test.ts` que ya existian.
`lib/menu.ts` exporta `MENU_ITEMS` (17 filas, todas con `agotado: false`), `getMenuItem`, `BY_ID` y
las 5 `CATEGORIES` viejas. `lib/menu-data.ts` exporta `MENU_CATEGORIES` con las 6 categorias reales.
Commit hecho.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mover el import debajo de "use client" en cart-context y descartar el KDS sin commitear de admin</name>
  <files>lib/cart-context.tsx, app/admin/page.tsx</files>
  <action>
Paso 1 — `lib/cart-context.tsx`: NO revertir el archivo. `formatPrice` (usado en
`buildWhatsAppUrl`) y la clave `lobo_cart_v2` se quedan tal cual. El unico cambio es de orden:
`"use client";` pasa a ser la primera sentencia del archivo, y el
`import { formatPrice } from "@/lib/utils";` baja al bloque de imports junto a los de
`react` y `@/lib/orders-store`. Next 16 exige que la directiva no tenga ninguna otra expresion
arriba; hoy queda degradada a un string statement suelto, por eso ESLint la marca como
`@typescript-eslint/no-unused-expressions` en la linea 2 y todas las rutas devuelven 500.

Paso 2 — `app/admin/page.tsx`: descartar los cambios sin commitear del KDS tactil con
`git checkout -- app/admin/page.tsx`. Esos cambios llaman a `/api/admin/puntos` y
`/api/admin/canjear`, endpoints que no existen (`app/api/admin/` solo tiene `pedidos/route.ts`), y
ademas arrastran tres bugs propios. El backup ya esta en `.context/kds-admin-page.tsx.bak` — NO lo
regeneres y no lo toques. Este paso no produce commit propio (solo restaura el arbol a HEAD);
verificalo con `git status --short app/admin/page.tsx`, que tiene que salir vacio.

Paso 3 — gate final. Los tres comandos, en este orden:
`npx tsc --noEmit && npx vitest run && npm run lint`
`npm run lint` va a seguir saliendo con exit code distinto de cero por los
`react-hooks/set-state-in-effect` preexistentes de `app/puntos/page.tsx` y `lib/cart-context.tsx`
— eso es la baseline aceptada y NO entra en alcance. Lo que si tiene que cumplirse: que
`no-unused-expressions` ya no aparezca y que ninguna regla nueva se haya sumado.
  </action>
  <verify>
    <automated>head -1 lib/cart-context.tsx | grep -q '"use client";'</automated>
    <automated>git status --short app/admin/page.tsx | grep -c . | grep -q '^0$'</automated>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run</automated>
    <automated>npm run lint 2>&amp;1 | grep -oE "@typescript-eslint/[a-z-]+|react-hooks/[a-z-]+" | sort -u   # unica linea esperada: react-hooks/set-state-in-effect</automated>
  </verify>
  <done>
La linea 1 de `lib/cart-context.tsx` es `"use client";` y el import de `formatPrice` esta en el
bloque de imports. `git status --short app/admin/page.tsx` sale vacio.
`npx tsc --noEmit` y `npx vitest run` verdes.
`npm run lint` no reporta `@typescript-eslint/no-unused-expressions` ni ninguna regla fuera de
`react-hooks/set-state-in-effect`. Commit hecho.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> `/api/charge` y `/api/culqi/order` | El cliente manda `{id, qty}`; el precio NUNCA viaja desde el cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3pr-01 | Tampering | `app/api/charge/route.ts` + `app/api/culqi/order/route.ts` | mitigate | Restaurar `getMenuItem` devuelve la fuente de precio al servidor. Los route handlers no se tocan: siguen recalculando el total con `getMenuItem(linea.id)` y siguen rechazando totales fuera de `MIN_CENTS`/`MAX_CENTS`. Verificado por los tests de caracterizacion y validacion de `/api/charge`, que vuelven a verde en Task 1. |
| T-3pr-02 | Tampering | Precios de `MENU_ITEMS` restaurados desde un commit viejo | accept | Son los mismos 17 productos/precios que estan cobrando en produccion hoy en `main`. La carta real de 30 productos entra por Supabase en el plan 02-02, no aqui. |
| T-3pr-03 | Denial of Service | Todas las rutas (`CartProvider` en `app/layout.tsx`) | mitigate | Task 2 devuelve la directiva `"use client"` a la primera linea; sin eso el 100% de las rutas (incluido el checkout que cobra) responde 500. |

Sin instalaciones de paquetes en este plan (npm/pip/cargo): no aplica el gate de legitimidad.
</threat_model>

<verification>
Gate obligatorio antes de cerrar:

```
npx tsc --noEmit && npx vitest run && npm run lint
```

- `tsc`: sin errores.
- `vitest run`: 0 fallos. Los 23 rojos con `TypeError: getMenuItem is not a function` desaparecen.
- `lint`: sale con exit != 0 por la baseline preexistente de `react-hooks/set-state-in-effect`.
  Lo que se exige es que la lista unica de reglas reportadas sea exactamente
  `react-hooks/set-state-in-effect`.

Chequeo manual opcional (no bloqueante): `npm run dev`, abrir `/` y confirmar que las 5 pestanas de
categoria muestran productos (ninguna vacia), y que `/admin` pide Basic Auth en vez de tirar 500.
</verification>

<success_criteria>
- `npx tsc --noEmit` verde
- `npx vitest run` verde, con `__tests__/menu.test.ts` presente y pasando
- `npm run lint` sin `@typescript-eslint/no-unused-expressions` y sin reglas nuevas
- `lib/menu.ts` exporta `MENU_ITEMS` (17 filas con `agotado: false`), `getMenuItem`, `BY_ID`,
  `CATEGORIES` = `["Combos", "Burgers", "Pollo", "Complementos", "Bebidas"]`
- `lib/menu-data.ts` exporta `MENU_CATEGORIES` con las 6 categorias reales, y nada mas cambio ahi
- `lib/cart-context.tsx` conserva `formatPrice` y `lobo_cart_v2`, con `"use client";` en la linea 1
- `git status --short` limpio para `app/admin/page.tsx`
- Ningun archivo de `<no_toques>` modificado
- 2 commits, uno por tarea
</success_criteria>

<output>
Crear `.planning/quick/260902-3pr-restaurar-build-verde-use-client-en-cart/260902-3pr-SUMMARY.md` al terminar.
</output>
