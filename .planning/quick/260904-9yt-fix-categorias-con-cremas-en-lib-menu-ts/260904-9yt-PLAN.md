---
phase: quick-260904-9yt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/menu.ts
  - __tests__/menu.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "CATEGORIAS_CON_CREMAS sigue valiendo exactamente [\"Combos\",\"Burgers\",\"Pollo\",\"Complementos\"] en runtime"
    - "Agregar una categoria nueva a CATEGORIES la incluye automaticamente en CATEGORIAS_CON_CREMAS sin tocar otra linea"
    - "El selector de cremas en app/page.tsx sigue apareciendo para Combos/Burgers/Pollo/Complementos y no para Bebidas"
    - "categoriaAdmiteCremas y CATEGORIAS_SIN_CREMAS quedan intactos (validacion server contra la taxonomia viva)"
  artifacts:
    - path: "lib/menu.ts"
      provides: "CATEGORIAS_CON_CREMAS derivado de CATEGORIES via filter"
      contains: "CATEGORIES.filter"
    - path: "__tests__/menu.test.ts"
      provides: "Test que congela el valor de CATEGORIAS_CON_CREMAS y prueba la derivacion"
  key_links:
    - from: "lib/menu.ts"
      to: "app/page.tsx"
      via: "import CATEGORIAS_CON_CREMAS"
      pattern: "CATEGORIAS_CON_CREMAS\\.includes"
---

<objective>
Derivar `CATEGORIAS_CON_CREMAS` de `CATEGORIES` con un `filter` de exclusion explicita en vez de mantener un array literal copiado a mano.

Purpose: hoy `CATEGORIAS_CON_CREMAS` (lib/menu.ts:24) duplica `CATEGORIES` (lib/menu.ts:21) menos "Bebidas". Si manana se agrega una categoria a `CATEGORIES` y nadie toca la otra constante, el selector de cremas del cliente (`app/page.tsx:72`) simplemente no aparece para esa categoria: sin error, sin warning, sin test rojo. Se pierde la eleccion de cremas del cliente en silencio.

Output: `lib/menu.ts` con la constante derivada + cobertura en `__tests__/menu.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@lib/menu.ts
@app/page.tsx
@__tests__/menu.test.ts

<interfaces>
Estado actual relevante de lib/menu.ts (lineas 21-37):

- `export const CATEGORIES = ["Combos", "Burgers", "Pollo", "Complementos", "Bebidas"];`
- `export const CATEGORIAS_CON_CREMAS = ["Combos", "Burgers", "Pollo", "Complementos"];`  <- objetivo del cambio
- `export const CATEGORIAS_SIN_CREMAS = ["Bebidas"];`  <- NO TOCAR
- `export function categoriaAdmiteCremas(category: string | null | undefined): boolean` <- NO TOCAR

Consumidores (grep verificado, lista completa):
- `app/page.tsx:12` importa `CATEGORIAS_CON_CREMAS`; `app/page.tsx:72` hace `CATEGORIAS_CON_CREMAS.includes(item.category)`
- `__tests__/menu.test.ts:5,37` importa `CATEGORIAS_CON_CREMAS` y la itera en el test de invariante deny-list vs allow-list
- `app/api/charge/route.ts` NO importa `CATEGORIAS_CON_CREMAS` (usa `categoriaAdmiteCremas`) — queda fuera de alcance

Ambos consumidores solo hacen `.includes(...)` / iteracion, asi que el tipo inferido `string[]` del `filter` es compatible sin cambios de tipo.
</interfaces>

Contexto importante (quick task 260904-9po): `CATEGORIAS_SIN_CREMAS` + `categoriaAdmiteCremas` son un deny-list deliberado para la taxonomia VIVA de la tabla `menu_items` en Postgres, que NO coincide con `CATEGORIES`. Son correctos y estan fuera de alcance. Este ticket toca unicamente la constante del CLIENTE contra el catalogo estatico.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derivar CATEGORIAS_CON_CREMAS de CATEGORIES</name>
  <files>lib/menu.ts, __tests__/menu.test.ts</files>
  <behavior>
    - `CATEGORIAS_CON_CREMAS` es exactamente `["Combos", "Burgers", "Pollo", "Complementos"]` (mismo contenido y mismo orden que hoy).
    - `CATEGORIAS_CON_CREMAS` no contiene `"Bebidas"`.
    - Toda categoria de `CATEGORIES` que no este en la lista de exclusion aparece en `CATEGORIAS_CON_CREMAS` (invariante de derivacion: `CATEGORIAS_CON_CREMAS.length === CATEGORIES.length - EXCLUIDAS.length`).
    - El test existente "es true para toda categoria de CATEGORIAS_CON_CREMAS" sigue pasando sin modificaciones.
  </behavior>
  <action>
    En `lib/menu.ts`, reemplazar el literal de la linea 24 por una derivacion de `CATEGORIES`.

    1. Declarar, inmediatamente antes de `CATEGORIAS_CON_CREMAS`, una constante de exclusion nueva y propia de esta derivacion. Nombre sugerido: `CATEGORIAS_ESTATICAS_SIN_CREMAS`, valor `["Bebidas"]`, marcada como `as const` o tipada `readonly string[]` segun se prefiera, y exportada solo si hace falta para el test (no hay otro consumidor).
       NO reutilizar `CATEGORIAS_SIN_CREMAS` (linea 32): es la deny-list de la taxonomia viva de Postgres del quick task 260904-9po, tiene otro proposito, esta declarada despues (reutilizarla obligaria a reordenar el modulo y a acoplar dos taxonomias que hoy son independientes a proposito).
    2. Definir `export const CATEGORIAS_CON_CREMAS = CATEGORIES.filter(c => !CATEGORIAS_ESTATICAS_SIN_CREMAS.includes(c));`. Mantener `CATEGORIES` declarado antes (ya lo esta, linea 21) para no caer en TDZ en la evaluacion del modulo.
    3. Actualizar el comentario de la linea 23 para explicar el porque de la derivacion: toda categoria nueva en `CATEGORIES` lleva cremas por defecto y hay que excluirla explicitamente para que no las lleve (fail-open deliberado del lado cliente; la validacion real vive en el server via `categoriaAdmiteCremas`).
    4. NO tocar `CATEGORIAS_SIN_CREMAS`, `categoriaAdmiteCremas`, ni el comentario de la linea 28-31 que los documenta.
    5. En `__tests__/menu.test.ts`, agregar dentro del `describe("lib/menu")` un caso que congele el valor exacto (`toEqual(["Combos","Burgers","Pollo","Complementos"])`) y un caso de invariante de derivacion (toda categoria de `CATEGORIES` esta en `CATEGORIAS_CON_CREMAS` salvo `"Bebidas"`). Importar lo que haga falta agregando al import existente de la linea 5, sin crear un import nuevo ni relativo.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run lint</automated>
  </verify>
  <done>
    - `grep -n 'CATEGORIES.filter' lib/menu.ts` da resultado.
    - `grep -n 'CATEGORIAS_SIN_CREMAS\|categoriaAdmiteCremas' lib/menu.ts` muestra ese bloque sin cambios respecto al merge-base (`git diff` de esas lineas vacio).
    - `npx tsc --noEmit` sin errores.
    - `npx vitest run` verde, incluyendo los tests nuevos y el test preexistente de invariante deny-list.
    - `npm run lint` en paridad con la baseline: exit 1 con exactamente los 3 errores `react-hooks/set-state-in-effect` de `app/puntos/page.tsx` y `lib/cart-context.tsx`, ninguna regla distinta. Medir la baseline del merge-base antes de comparar, no asumirla.
    - Salida real de los tres comandos pegada en el SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> /api/charge | El cliente elige cremas; el servidor valida que la categoria las admita |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9yt-01 | Tampering | `CATEGORIAS_CON_CREMAS` (cliente) | accept | Constante de UI, no de autorizacion. La validacion server-side sigue siendo `categoriaAdmiteCremas` en `app/api/charge/route.ts:129`, intacta en este cambio. Un cliente manipulado ya no podia saltearse nada por esta via. |
| T-9yt-SC | Tampering | instalaciones npm/pip/cargo | n/a | El plan no instala paquetes nuevos. |
</threat_model>

<verification>
- `git diff lib/menu.ts` toca solo el bloque de lineas 23-24 (mas la constante de exclusion nueva); el bloque `CATEGORIAS_SIN_CREMAS`/`categoriaAdmiteCremas` queda byte-identico.
- El valor en runtime de `CATEGORIAS_CON_CREMAS` no cambia (test de congelamiento verde).
- `app/page.tsx` no requiere cambios.
</verification>

<success_criteria>
- `CATEGORIAS_CON_CREMAS` derivada de `CATEGORIES` via `filter` con exclusion explicita.
- Valor de runtime identico al actual, probado por test.
- Gate `npx tsc --noEmit && npx vitest run && npm run lint` corrido y con salida pegada, lint en paridad con baseline.
- Cero dependencias nuevas, cero cambios en la validacion server.
</success_criteria>

<output>
Crear `.planning/quick/260904-9yt-fix-categorias-con-cremas-en-lib-menu-ts/260904-9yt-SUMMARY.md` al terminar.
</output>
