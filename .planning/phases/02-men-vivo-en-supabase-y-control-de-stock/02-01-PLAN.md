---
phase: 02-men-vivo-en-supabase-y-control-de-stock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - supabase/migrations/20260901000000_menu_items.sql
  - lib/menu.ts
  - lib/menu-data.ts
  - __tests__/menu.test.ts
  - __tests__/menu-data.test.ts
  - __tests__/helpers/supabase-mock.ts
autonomous: false
requirements: [MENU-01, MENU-02, MENU-04, OPS-04]

must_haves:
  truths:
    - "La tabla menu_items existe en el Supabase remoto con las 30 filas de la carta real de Jaime"
    - "Lobo Sunset 350ml está sembrado como agotado desde el arranque"
    - "Un módulo server-only expone la lista cacheada de la carta y la lectura viva por id, en funciones distintas"
    - "getMenuItemLive nunca pasa por unstable_cache: el precio que devuelve es siempre el vigente en la base"
  artifacts:
    - path: "supabase/migrations/20260901000000_menu_items.sql"
      provides: "create table menu_items + seed de la carta real"
      contains: "create table menu_items"
    - path: "lib/menu.ts"
      provides: "Tipos puros importables desde \"use client\" (MenuItem con agotado, CATEGORIES)"
      exports: ["MenuItem", "CATEGORIES"]
    - path: "lib/menu-data.ts"
      provides: "Capa de datos server-only del menú"
      exports: ["getMenuItemsCached", "getMenuItemLive", "updateMenuItem"]
    - path: "__tests__/menu-data.test.ts"
      provides: "Cobertura de mapeo, lectura viva y escritura+invalidación"
  key_links:
    - from: "lib/menu-data.ts"
      to: "getSupabaseAdmin"
      via: "import desde @/lib/supabase (mismo lazy singleton, no un cliente nuevo)"
      pattern: "getSupabaseAdmin\\(\\)"
    - from: "lib/menu-data.ts"
      to: "unstable_cache"
      via: "solo envuelve el listado público, con tags:['menu'] y revalidate:false"
      pattern: "unstable_cache\\("
    - from: "updateMenuItem"
      to: "revalidateTag"
      via: "revalidateTag('menu', { expire: 0 }) en el mismo request del UPDATE"
      pattern: "revalidateTag\\(\"menu\", \\{ expire: 0 \\}\\)"
---

<objective>
Mover la carta de un array estático en `lib/menu.ts` a la tabla `menu_items` de Supabase,
sembrada con la carta real confirmada por el dueño, y crear la capa de datos server-only que
separa físicamente la lectura cacheada (carta pública) de la lectura viva (cobro).

Purpose: MENU-01 y MENU-04 dependen estructuralmente de esta separación. Todo lo demás del
phase (carta pública, cobro contra precio vivo, edición en `/admin`) se construye encima.

Output: migración aplicada en el Supabase remoto, `lib/menu.ts` reducido a tipos,
`lib/menu-data.ts` nuevo con las tres funciones, tests en verde.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-CONTEXT.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-PATTERNS.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del código real. -->

lib/supabase.ts (sin cambios, reusar tal cual):
  export function getSupabaseAdmin(): SupabaseClient

lib/menu.ts (estado ACTUAL, a reescribir):
  export type MenuItem = { id, category, name, description, price, badge, originalPrice, image }
  export const CATEGORIES = ["Combos", "Burgers", "Pollo", "Complementos", "Bebidas"]
  export const MENU_ITEMS: MenuItem[]   -> se ELIMINA
  export function getMenuItem(id: number): MenuItem | undefined   -> se ELIMINA

next/cache (verificado contra node_modules/next/dist/server/web/spec-extension/revalidate.d.ts):
  export declare function revalidateTag(tag: string, profile: string | { expire?: number }): undefined
  El segundo argumento NO es opcional.

next/cache unstable_cache (verificado contra
node_modules/next/dist/server/web/spec-extension/unstable-cache.js):
  - `revalidate: 0` lanza Invariant E57. Usar `revalidate: false`.
  - Llamar la función devuelta FUERA de un request de Next lanza
    "Invariant: incrementalCache missing in unstable_cache" -> los tests DEBEN mockear
    "next/cache", no pueden ejecutar getMenuItemsCached contra el runtime real.

__tests__/helpers/supabase-mock.ts (estado ACTUAL, soporta):
  .from(t).insert(row).select().single()
  .from(t).select().eq(col, val).single()      <- ya sirve para getMenuItemLive
  .from(t).select().limit(n)                   <- thenable directo
  .rpc(fn, params)
  NO soporta todavía: .select().order().order()  ni  .update(patch).eq(col, val)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar server-only y escribir la migración con la carta real</name>
  <files>package.json, package-lock.json, supabase/migrations/20260901000000_menu_items.sql</files>
  <read_first>
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-CONTEXT.md (sección `<specifics>` — la carta real es la fuente de verdad literal del seed)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md (Code Examples → "Migración SQL"; Package Legitimacy Audit)
    - supabase/migrations/20260825000000_rate_limit.sql (convención: comentario del "por qué", RLS habilitada sin políticas)
    - supabase/migrations/20260820000000_pedidos.sql (convención de columnas y de `_centimos integer`)
  </read_first>
  <action>
Ejecutar `npm install server-only` (queda en `dependencies`, no en devDependencies). El paquete
está aprobado en el Package Legitimacy Audit de 02-RESEARCH.md con veredicto [OK] de slopcheck —
no requiere checkpoint de legitimidad.

Crear `supabase/migrations/20260901000000_menu_items.sql` con la misma convención de los dos
migrations existentes: comentario de cabecera explicando el "por qué" antes del DDL, y
`alter table menu_items enable row level security;` sin políticas (la tabla solo la toca el
service_role desde el servidor, igual que `pedidos` y `rate_limit_charge`; no existe
`NEXT_PUBLIC_SUPABASE_ANON_KEY` en este proyecto).

Columnas exactas de `create table menu_items`:
`id bigint generated always as identity primary key`, `created_at timestamptz not null default now()`,
`category text not null`, `name text not null`, `description text not null default ''`,
`precio_centimos integer not null check (precio_centimos > 0)`, `original_price_centimos integer`,
`badge text`, `image text`, `agotado boolean not null default false`.
Más `create index menu_items_category_idx on menu_items (category, id);`.

El comentario de cabecera debe explicar por qué `precio_centimos integer` y no `numeric`:
PostgREST serializa `numeric` como string para no perder precisión decimal, y supabase-js lo
devuelve como `"17.90"`, no como number — rompería en silencio la aritmética de `/api/charge`.
Un entero en céntimos replica `pedidos.total_centimos` y elimina el `Math.round(price * 100)`
que hoy hace el route handler.

Seed: un solo `insert into menu_items (category, name, description, precio_centimos, image, agotado) values`
con exactamente 30 filas, UNA FILA POR LÍNEA, cada línea empezando con dos espacios y `('`.
Los valores salen literalmente de `<specifics>` de 02-CONTEXT.md, convirtiendo soles a céntimos
(S/17.90 -> 1790). `original_price_centimos` y `badge` quedan NULL en las 30 filas (la carta real
no trae promos; las columnas quedan disponibles sin necesitar otra migración).

Los valores de `category` deben ser EXACTAMENTE estos seis strings, y son los mismos que va a
listar `CATEGORIES` en la Task 3: "Enchiladas", "Broaster", "Salchipapas / Power Plates",
"Combos xtremos", "Bebidas", "Hamburguesas".

Filas por categoría (nombre, céntimos, descripción):
Enchiladas — "Enchilada de Pollo" 1790 "pollo deshilachado, chorizo parrillero";
"Enchilada de Chorizo" 1790 "chorizo parrillero"; "Enchilada Mixta" 1990 "pollo deshilachado + chorizo parrillero";
"Enchibestia" 2090 "pollo deshilachado, cabanossi, tocino".
Broaster — "Broaster Lobito" 1450 "1 pieza de pollo (ala o pierna), papas fritas, ensalada, cremas";
"Broaster Lobo" 1690 "1 pieza de pollo (pecho o entrepierna), papas o papas con arroz, ensalada, cremas".
Salchipapas / Power Plates — "Salchibasic" 1250 "papas fritas, frankfurter, cremas";
"Salchipobre" 1490 "papas fritas, frankfurter, huevo, plátano frito, cremas";
"Perro Lobo" 1290 "frankfurter, tocino, queso, papas al hilo, huevo, plátano frito, cremas";
"El Breakfast del Lobo" 1990 "pechugón a la plancha, papas fritas, huevo, plátano frito, cremas".
Combos xtremos — "Combo Resuelve" 1690 "1 pieza de pollo (ala o pierna), papas fritas, ensalada, cremas, Coca-Cola 296ml";
"Combo Instinto" 2190 "chorizo parrillero, pollo deshilachado, queso, papas al hilo, ensalada, cremas, Coca-Cola 296ml";
"Combo Royal" 1850 "hamburguesa casera, huevo, queso, papas fritas o al hilo, ensalada, cremas, Lobo Sunset 350ml".
Bebidas — "Guaraná 450ml" 400 ''; "Coca-Cola 296ml" 400 ''; "Inca Kola 296ml" 400 '';
"Fanta Naranja 500ml" 450 ''; "Fanta Kola Inglesa 500ml" 450 ''; "Agua San Luis 500ml" 350 '';
"Lobo Sunset 350ml" 390 "refresco de la casa".
Hamburguesas — "Classic" 1350 ''; "Cheeseburger" 1490 ''; "Hamburguesa Royal" 1650 '';
"Deshilachado Royal" 1690 ''; "Filete de Pollo Royal" 1690 "sin queso"; "Bacon Cheeseburger" 1690 '';
"ChoriRoyal" 1690 ''; "Double Double" 1990 ''; "Burgazo" 2250 ''; "Tropical Burguer" 1790 ''.

`agotado = true` en EXACTAMENTE una fila: "Lobo Sunset 350ml" (decisión del dueño en 02-CONTEXT.md,
es el caso real para validar OPS-04, no un dato sintético). Las otras 29 van `false`.

`image` no nulo en EXACTAMENTE 7 filas, con las fotos reales que ya existen en el repo y que
corresponden 1:1 al producto (verificadas visualmente durante el planning):
"Salchipobre" -> '/images/menu/salchipobre.webp';
"El Breakfast del Lobo" -> '/images/menu/el-breakfast-del-lobo.webp';
"Filete de Pollo Royal" -> '/images/menu/filete-de-pollo-royal.webp';
"Bacon Cheeseburger" -> '/images/menu/baconcheese.webp';
"ChoriRoyal" -> '/images/menu/choriroyal.webp';
"Burgazo" -> '/images/menu/burgazo.webp';
"Tropical Burguer" -> '/images/menu/tropical-burger.webp'.
Las otras 23 van `null` y se pintan con el ícono por categoría (plan 02-03). Dejar un comentario
en el SQL explicando que el reemplazo de ícono por foto es incremental producto por producto:
basta un UPDATE de la columna `image`, no hay que tocar nada más.

Escapar los apóstrofes del SQL donde aplique. NO truncar el seed ni dejar un `-- ... resto`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260901000000_menu_items.sql && grep -v "^--" supabase/migrations/20260901000000_menu_items.sql | grep -c "^  ('" | grep -qx 30 && grep -c "server-only" package.json | grep -qx 1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls server-only` resuelve sin error y `server-only` aparece bajo `dependencies` en package.json
    - El archivo de migración tiene exactamente 30 líneas de fila de seed (`^  ('`) ignorando comentarios
    - `grep -c "agotado" ` sobre las filas del seed muestra `true` en la línea de "Lobo Sunset 350ml" y en ninguna otra
    - `grep -c "/images/menu/" ` sobre las filas del seed devuelve 7
    - El archivo contiene `create table menu_items`, `precio_centimos integer not null check (precio_centimos > 0)`, `create index menu_items_category_idx` y `alter table menu_items enable row level security;`
    - Los seis strings de categoría del seed son exactamente los seis listados en el action, sin variantes de acentuación ni espaciado
  </acceptance_criteria>
  <done>La migración existe con la carta real completa y `server-only` está instalado.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar la migración al Supabase remoto y verificar el seed</name>
  <files>supabase/migrations/20260901000000_menu_items.sql</files>
  <read_first>
    - supabase/migrations/20260901000000_menu_items.sql (el archivo escrito en la Task 1)
    - .env.local (confirmar que `SUPABASE_URL` y `SUPABASE_SERVICE_ROLE_KEY` están presentes; NO imprimir sus valores)
  </read_first>
  <action>
TAREA BLOQUEANTE: sin esto, `npm run build` y los type-checks pasan igual (los tipos salen del
código, no de la base) y la verificación del phase daría un falso positivo. Nada del resto del
phase puede verificarse contra la base real hasta que esta tarea termine en verde.

El proyecto YA está vinculado (existe `supabase/.temp` con el project-ref `kkkdfciwwqbfkapgaoov`).
Correr `supabase db push --linked --yes`. El flag `--yes` suprime la confirmación interactiva.

Si el CLI pide la contraseña de Postgres y no está en el keyring, NO adivinar ni improvisar:
fallback en este orden, y detenerse a pedir intervención humana si los dos fallan.
1. Aplicar el mismo SQL vía el servidor MCP de Supabase (`apply_migration`, nombre de migración
   `menu_items`), que es completamente no interactivo.
2. Si tampoco está disponible, parar y reportar que la tarea necesita la contraseña del proyecto.

Después del push, verificar contra el Supabase REMOTO (no local) que la tabla quedó sembrada:
30 filas, suma de `precio_centimos` = 42670, exactamente 1 fila con `agotado = true`,
exactamente 7 filas con `image` no nulo, 6 valores distintos de `category`. Usar
`node --env-file=.env.local --input-type=module -e` con `@supabase/supabase-js` (Node 20.19
soporta `--env-file`), o el `execute_sql` del MCP de Supabase. La comprobación debe salir por
stdout y devolver exit code distinto de 0 si algún conteo no cuadra.

No borrar ni modificar `lib/menu.ts` en esta tarea — la app sigue funcionando contra el array
estático hasta la Task 3. La ventana en la que la tabla existe pero nadie la lee es inofensiva.
  </action>
  <verify>
    <automated>supabase migration list --linked 2>/dev/null | grep -q "20260901000000" && node --env-file=.env.local --input-type=module -e "import{createClient}from '@supabase/supabase-js';const c=createClient(process.env.SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY);const{data,error}=await c.from('menu_items').select('precio_centimos,agotado,image,category');if(error)throw error;const suma=data.reduce((a,r)=>a+r.precio_centimos,0);const ago=data.filter(r=>r.agotado).length;const img=data.filter(r=>r.image).length;const cat=new Set(data.map(r=>r.category)).size;console.log({filas:data.length,suma,ago,img,cat});if(data.length!==30||suma!==42670||ago!==1||img!==7||cat!==6)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` muestra `20260901000000` aplicada en remoto
    - La query de verificación imprime `{filas:30, suma:42670, ago:1, img:7, cat:6}` y sale con código 0
    - `select name from menu_items where agotado` devuelve exactamente `Lobo Sunset 350ml`
    - Ningún archivo de la app fue modificado en esta tarea
  </acceptance_criteria>
  <done>La tabla `menu_items` existe en el Supabase de producción con las 30 filas de la carta real.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Capa de datos server-only — cacheada para la carta, viva para el cobro</name>
  <files>lib/menu.ts, lib/menu-data.ts, __tests__/menu-data.test.ts, __tests__/menu.test.ts, __tests__/helpers/supabase-mock.ts</files>
  <read_first>
    - lib/menu.ts (archivo a reescribir; conservar el espíritu del comentario de cabecera)
    - lib/supabase.ts (patrón del lazy singleton `getSupabaseAdmin`, reusar sin crear otro cliente)
    - app/api/admin/pedidos/route.ts (forma de las queries `.select().order()` y `.update().eq()`, y el `if (error) throw error;`)
    - __tests__/helpers/supabase-mock.ts (mock encadenable a extender de forma aditiva)
    - __tests__/api-charge.caracterizacion.test.ts (patrón de `vi.mock` de módulo + `useSupabaseMock`)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 3)
  </read_first>
  <behavior>
    - `rowToMenuItem` convierte `precio_centimos: 1790` en `price: 17.9` y `original_price_centimos: null` en `originalPrice: null`
    - `getMenuItemsCached()` devuelve las filas mapeadas a `MenuItem[]`, consultando `menu_items` con lista explícita de columnas y ordenando por `category` y luego por `id`
    - `getMenuItemsCached()` propaga el error de Supabase (throw) en vez de devolver lista vacía
    - `getMenuItemLive(id)` devuelve `{ id, precio_centimos, agotado }` de la fila consultada por `.eq("id", id).single()`
    - `getMenuItemLive(id)` devuelve `undefined` cuando Supabase responde con error o sin fila (id inexistente)
    - `getMenuItemLive` devuelve el `precio_centimos` crudo, sin dividir entre 100 (el cobro es aritmética entera)
    - `updateMenuItem(id, { precio_centimos })` llama `.from("menu_items").update({precio_centimos}).eq("id", id)` y luego `revalidateTag("menu", { expire: 0 })`
    - `updateMenuItem` NO llama `revalidateTag` si el UPDATE devolvió error (lanza antes)
  </behavior>
  <action>
RED primero: escribir `__tests__/menu-data.test.ts` completo contra el comportamiento del bloque
`<behavior>`, correrlo y confirmar que falla, y recién después escribir `lib/menu-data.ts`.

El test necesita dos mocks de módulo obligatorios, ambos por razones verificadas en el planning:
`vi.mock("server-only", () => ({}))` porque el paquete lanza al importarse fuera de la condición
de resolución `react-server`, y `vi.mock("next/cache", ...)` devolviendo `unstable_cache: (fn) => fn`
y `revalidateTag: vi.fn()` porque llamar la función devuelta por el `unstable_cache` real fuera de
un request de Next lanza "Invariant: incrementalCache missing". Con ese mock, `getMenuItemsCached`
queda siendo la función interna y sí es testeable. El archivo lleva el docblock
`// @vitest-environment node` como el resto de los tests de servidor.

Extender `__tests__/helpers/supabase-mock.ts` de forma ADITIVA (sin romper las cadenas que ya
usan los planes 01-03 a 01-08), con un comentario explicando que el plan 02-01 las necesita:
(a) `.select(cols).order(col).order(col)` — el objeto devuelto por `.order()` debe exponer otro
`.order()` y ser thenable, resolviendo a `config.selectOrderResult ?? { data: [], error: null }`;
registrar los argumentos en `calls.selectOrderArgs`.
(b) `.update(patch).eq(col, value)` — thenable que resuelve a `config.updateResult ?? { error: null }`;
registrar `calls.updateArgs` (el patch) y `calls.updateEqArgs` (columna y valor).
Agregar los campos nuevos a `SupabaseMockConfig` y a `SupabaseMockCalls`.

Reescribir `lib/menu.ts` dejándolo SOLO con tipos y constantes, sin ningún import de Supabase, para
que siga siendo importable desde `"use client"`. Exporta `type MenuItem` con los campos actuales
(`id, category, name, description, price, badge, originalPrice, image`) MÁS `agotado: boolean`, y
`export const CATEGORIES` con los seis strings en el orden del documento fuente:
"Enchiladas", "Broaster", "Salchipapas / Power Plates", "Combos xtremos", "Bebidas", "Hamburguesas"
(orden de la carta física; es Claude's Discretion documentada en 02-CONTEXT.md). Borrar
`MENU_ITEMS`, `BY_ID` y `getMenuItem`. Actualizar el comentario de cabecera: este archivo ya no es
la fuente de verdad del precio — la fuente es la tabla `menu_items`; acá solo viven los tipos y el
orden de categorías de la UI, y por eso sigue siendo seguro importarlo desde el navegador.

Crear `lib/menu-data.ts` con `import "server-only";` en la PRIMERA línea. Exporta:
- `getMenuItemsCached`: `unstable_cache` sobre una función interna no exportada que consulta
  `menu_items` con `.select("id, category, name, description, precio_centimos, badge, original_price_centimos, image, agotado")`
  (lista explícita, nunca `select("*")`: el endpoint es público y una columna interna futura no
  debe filtrarse), `.order("category")`, `.order("id")`, `if (error) throw error;` y mapeo con
  `rowToMenuItem`. keyParts `["menu-items-public"]`, opciones `{ tags: ["menu"], revalidate: false }`.
  `revalidate: 0` lanza Invariant E57 en esta versión de Next — usar `false`.
- `getMenuItemLive(id: number)`: `async function` normal, SIN ningún wrapper de caché, con
  `.select("id, precio_centimos, agotado").eq("id", id).single()`; devuelve `undefined` si hay
  error o no hay fila. Comentario encima dejando explícito que envolver esta función en
  `unstable_cache` rompe MENU-04, y que las únicas dos rutas que la llaman son
  `app/api/charge/route.ts` y `app/api/culqi/order/route.ts`.
- `updateMenuItem(id: number, patch: { precio_centimos?: number; agotado?: boolean })`:
  `.update(patch).eq("id", id)`, `if (error) throw error;` y después `revalidateTag("menu", { expire: 0 })`.
  La forma de dos argumentos es obligatoria: la firma publicada en
  node_modules/next/dist/server/web/spec-extension/revalidate.d.ts declara `profile` como
  requerido, y la forma de un argumento está deprecada.

Borrar `__tests__/menu.test.ts` (probaba `getMenuItem`/`MENU_ITEMS` síncronos, que dejan de existir);
`__tests__/menu-data.test.ts` lo reemplaza.

NO tocar `app/api/charge/route.ts` ni `app/api/culqi/order/route.ts` en esta tarea — quedan rotos a
nivel de import hasta el plan 02-02, que es de la misma tanda de despliegue. Es esperado.
  </action>
  <verify>
    <automated>npx vitest run __tests__/menu-data.test.ts && test ! -f __tests__/menu.test.ts && grep -v "^\s*//" lib/menu.ts | grep -c "MENU_ITEMS\|getMenuItem\|supabase" | grep -qx 0 && grep -v "^\s*//" lib/menu-data.ts | grep -q "export async function getMenuItemLive" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/menu-data.test.ts` pasa, cubriendo los 8 comportamientos del bloque `<behavior>`
    - `lib/menu-data.ts` tiene `import "server-only";` en la línea 1
    - `getMenuItemLive` está declarada como `export async function` y NO aparece dentro de ninguna llamada a `unstable_cache` (verificable: la única aparición de `unstable_cache(` en el archivo envuelve la función del listado)
    - `lib/menu.ts` no contiene `MENU_ITEMS`, `getMenuItem` ni ningún import de `@/lib/supabase` fuera de comentarios
    - `CATEGORIES` tiene los 6 strings, en el orden del documento fuente, idénticos a los valores de `category` sembrados en la Task 1
    - `updateMenuItem` invoca `revalidateTag("menu", { expire: 0 })` con dos argumentos
    - El mock de Supabase soporta las cadenas nuevas sin romper ningún test preexistente que lo use
  </acceptance_criteria>
  <done>La capa de datos existe con la función cacheada y la viva separadas, y los tests están en verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `/api/*` | Todo lo que cruza es no confiable; el precio nunca viaja desde acá |
| app (service_role) → Postgres | La app tiene poderes totales sobre `menu_items`; RLS no protege al service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `getMenuItemLive` envuelto por error en `unstable_cache` (precio stale al cobrar) | mitigate | Separación por nombre y comentario en `lib/menu-data.ts`; gate de grep en el plan 02-02 verifica que las rutas de cobro no importen `getMenuItemsCached` |
| T-02-02 | Information Disclosure | `menu_items` expuesto públicamente con columnas internas futuras | mitigate | `select()` con lista explícita de columnas en `getMenuItemsCached`, nunca `select("*")` |
| T-02-03 | Elevation of Privilege | Cliente hablando directo con Supabase | mitigate | `import "server-only"` en `lib/menu-data.ts` hace fallar el build si alguien lo importa desde `"use client"`; no existe `NEXT_PUBLIC_SUPABASE_ANON_KEY` en el proyecto |
| T-02-04 | Tampering | RLS deshabilitada dejaría la tabla legible con la anon key | mitigate | `alter table menu_items enable row level security;` sin políticas, igual que `pedidos` y `rate_limit_charge` |
| T-02-SC | Tampering | Instalación de `server-only` vía npm | mitigate | Auditado en 02-RESEARCH.md § Package Legitimacy Audit: veredicto [OK] de slopcheck, 0 dependencias, sin `postinstall`, referenciado por la documentación de Next instalada en el repo. No requiere checkpoint humano. |
</threat_model>

<verification>
1. `supabase migration list --linked` incluye `20260901000000`
2. La query de conteo devuelve `{filas:30, suma:42670, ago:1, img:7, cat:6}`
3. `npx vitest run __tests__/menu-data.test.ts` en verde
4. `npm run lint` sin errores nuevos
5. `npx tsc --noEmit` reporta solo los errores esperados en `app/api/charge/route.ts`,
   `app/api/culqi/order/route.ts` y `app/page.tsx` (importan símbolos que ya no existen; los
   arreglan los planes 02-02 y 02-03 en la misma tanda de despliegue)
</verification>

<success_criteria>
- La tabla `menu_items` está en el Supabase remoto con las 30 filas de la carta real y "Lobo Sunset 350ml" agotado
- `lib/menu.ts` es un archivo de tipos puro, sin datos ni acceso a Supabase
- `lib/menu-data.ts` expone `getMenuItemsCached` (cacheada, tag `menu`), `getMenuItemLive` (siempre viva) y `updateMenuItem` (escribe + invalida)
- El helper de mock de Supabase soporta `.order().order()` y `.update().eq()` sin romper tests existentes
</success_criteria>

<output>
Create `.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-01-SUMMARY.md` when done
</output>
</content>
</invoke>
