---
phase: 02-men-vivo-en-supabase-y-control-de-stock
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - lib/menu-data.ts
  - __tests__/menu-data.test.ts
  - __tests__/helpers/menu-data-mock.ts
  - app/api/charge/route.ts
  - app/api/culqi/order/route.ts
  - __tests__/api-charge.precio-vivo.test.ts
  - __tests__/api-charge.caracterizacion.test.ts
  - __tests__/api-charge.alertas.test.ts
  - __tests__/api-charge.metadata.test.ts
  - __tests__/api-charge.rate-limit.test.ts
  - __tests__/api-charge.validacion.test.ts
  - __tests__/api-culqi-order.test.ts
autonomous: true
requirements: [MENU-04, OPS-04]

must_haves:
  truths:
    - "El total que se cobra en /api/charge sale del precio vigente en menu_items, leído en el mismo request, nunca de un array estático ni de una entrada de caché"
    - "Un pedido que incluye un producto con agotado = true es rechazado con 400 por /api/charge antes de llamar a Culqi"
    - "Un pedido que incluye un producto agotado es rechazado con 400 por /api/culqi/order antes de crear la Orden en Culqi"
    - "El monto que el navegador manda (amount/total) sigue siendo ignorado: la antiregresión del exploit de S/3 sigue en verde"
    - "El total se calcula con aritmética entera sobre precio_centimos, sin Math.round sobre floats"
  artifacts:
    - path: "__tests__/helpers/menu-data-mock.ts"
      provides: "Catálogo de prueba + factory de mock de @/lib/menu-data reusable por todos los tests de cobro"
      exports: ["CATALOGO_TEST", "menuDataMock"]
    - path: "__tests__/api-charge.precio-vivo.test.ts"
      provides: "Cobertura de MENU-04 y OPS-04 en /api/charge"
    - path: "__tests__/api-culqi-order.test.ts"
      provides: "Cobertura de MENU-04 y OPS-04 en /api/culqi/order"
  key_links:
    - from: "app/api/charge/route.ts"
      to: "getMenuItemLive"
      via: "import desde @/lib/menu-data, await dentro del loop de items"
      pattern: "await getMenuItemLive\\("
    - from: "app/api/culqi/order/route.ts"
      to: "getMenuItemLive"
      via: "import desde @/lib/menu-data, await dentro del loop de items"
      pattern: "await getMenuItemLive\\("
    - from: "app/api/charge/route.ts"
      to: "rechazo de agotado"
      via: "return 400 inmediatamente después del chequeo de !item"
      pattern: "item\\.agotado"
---

<objective>
Mover el recálculo del total de las dos rutas de cobro (`app/api/charge/route.ts` y
`app/api/culqi/order/route.ts`) del array estático de `lib/menu.ts` a la lectura viva
`getMenuItemLive()` de `lib/menu-data.ts`, y hacer que ambas rechacen explícitamente los
productos marcados `agotado`.

Purpose: es el corazón de MENU-04 (nunca cobrar un precio cacheado) y el lado autoritativo de
OPS-04 (el botón deshabilitado del navegador no es una defensa). Va ANTES que la carta pública
porque el guardrail 4 exige cerrar las rutas de API y los tipos antes de tocar la pantalla, y
porque el borrado de `MENU_ITEMS` (plan 02-04) no puede pasar hasta que estas dos rutas dejen
de importarlo.

Output: dos rutas de cobro leyendo de Supabase en cada request, batería de tests de cobro
migrada a un catálogo de prueba con los ids nuevos (>= 1001) y dos suites nuevas que cubren
precio vivo + rechazo de agotado.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-CONTEXT.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-PATTERNS.md

<interfaces>
<!-- Contratos reales del repo al momento de planificar. No explorar el codebase para esto. -->

lib/menu-data.ts (existe, creado por el plan 02-01):
  export const MENU_CATEGORIES: readonly string[]   // se elimina en el plan 02-04, no tocar acá
  export const getMenuItemsCached: () => Promise<MenuItem[]>          // cacheada, NO usar acá
  export async function getMenuItemLive(id: number):
      Promise<{ id: number; precio_centimos: number; agotado: boolean } | undefined>
  export async function updateMenuItem(id: number, patch): Promise<void>

lib/menu.ts (estado ACTUAL — sigue teniendo el array estático, NO se toca en este plan):
  export type MenuItem = { id, category, name, description, price, badge, originalPrice, image, agotado }
  export const CATEGORIES: string[]
  export const MENU_ITEMS: MenuItem[]
  export function getMenuItem(id: number): MenuItem | undefined

app/api/charge/route.ts (loop actual, líneas ~96-118):
  const item = getMenuItem(linea.id);
  if (!item) -> 400 "Hay un producto que ya no está disponible"
  totalCents += Math.round(item.price * 100) * linea.qty;
  detalle.push({ id: item.id, name: item.name, price: item.price, qty: linea.qty });
  Constantes locales: MIN_CENTS = 300, MAX_CENTS = 50000, MAX_QTY = 20

app/api/culqi/order/route.ts (loop actual, líneas 52-69): mismo patrón, sin `detalle`.
  Constantes locales: MIN_CENTS = 600, MAX_CENTS = 50000, MAX_QTY = 20

__tests__/api-charge.caracterizacion.test.ts (patrón de mock a imitar):
  // @vitest-environment node   <- primera línea, no borrar
  vi.mock("@/lib/supabase", () => ({ getSupabaseAdmin: vi.fn() }));
  import { POST } from "@/app/api/charge/route";
  function bodyValido(overrides) { ... items: [{ id: 1, qty: 1 }] ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getMenuItemLive devuelve el nombre + helper de mock del catálogo de prueba</name>
  <files>lib/menu-data.ts, __tests__/menu-data.test.ts, __tests__/helpers/menu-data-mock.ts</files>
  <read_first>
    - lib/menu-data.ts (archivo a modificar; conservar el bloque de comentario "ADVERTENCIA CRÍTICA DE INTEGRIDAD (MENU-04)" tal cual)
    - __tests__/menu-data.test.ts (test a actualizar; ver el bloque que arma `row` y el `expect(res).toEqual(...)` de getMenuItemLive)
    - app/api/charge/route.ts (leer el `detalle.push({ id, name, price, qty })` para entender por qué hace falta `name`)
    - __tests__/helpers/supabase-mock.ts (estilo de los helpers de test de este repo: config tipada + registro de llamadas)
  </read_first>
  <behavior>
    - `getMenuItemLive(1001)` devuelve `{ id: 1001, name: "Enchilada de Pollo", precio_centimos: 1790, agotado: false }`
    - `getMenuItemLive` sigue devolviendo `undefined` cuando Supabase responde con error o sin fila
    - `getMenuItemLive` sigue sin pasar por ningún wrapper de caché
    - `menuDataMock().getMenuItemLive(1020)` devuelve la entrada agotada del catálogo de prueba
    - `menuDataMock().getMenuItemLive(9999)` devuelve `undefined` (id fuera del catálogo)
  </behavior>
  <action>
En `lib/menu-data.ts`, agregar `name` a `getMenuItemLive`: cambiar el `.select("id, precio_centimos, agotado")`
por `.select("id, name, precio_centimos, agotado")`, agregar `name: string` al tipo de retorno declarado
(`Promise<{ id: number; name: string; precio_centimos: number; agotado: boolean } | undefined>`) y devolver
`name: data.name` en el objeto de retorno. Motivo, dejarlo en un comentario de una línea: `/api/charge`
guarda `detalle` (id, name, price, qty) en `pedidos.items` y ese nombre es lo que ve la cocina y el panel;
sin el nombre en la lectura viva habría que volver a leer la fila desde otro lado. NO agregar ninguna otra
columna: sigue siendo una lista explícita, no `select("*")`.

En `__tests__/menu-data.test.ts`, actualizar el caso de `getMenuItemLive`: agregar `name` a la fila mockeada
y al `expect(...).toEqual(...)`, y agregar el assert de que el string pasado a `.select()` contiene `name`.
No cambiar ningún otro caso del archivo.

Crear `__tests__/helpers/menu-data-mock.ts` (nuevo, sin `// @vitest-environment`, es un helper). Exporta:

- `type ItemCatalogo = { id: number; name: string; precio_centimos: number; agotado: boolean }`
- `export const CATALOGO_TEST: ItemCatalogo[]` con exactamente estas 6 entradas. Comentario arriba
  explicando que los ids son fixtures arbitrarios >= 1001 (la secuencia real arranca en 1001) y que NO
  deben asumirse iguales a los ids de producción:
  `1001 "Enchilada de Pollo" 1790 false`,
  `1015 "Coca-Cola 296ml" 400 false`,
  `1020 "Lobo Sunset 350ml" 390 true`,
  `1021 "Agua San Luis 500ml" 350 false`,
  `1029 "Double Double" 1990 false`,
  `1030 "Burgazo" 2250 false`.
- `export function menuDataMock(overrides?: ItemCatalogo[])` que devuelve un objeto con
  `getMenuItemLive: vi.fn(async (id: number) => (overrides ?? CATALOGO_TEST).find((i) => i.id === id))`.
  Importar `vi` desde `"vitest"`.

El helper NO debe importar `@/lib/menu-data` ni `server-only` — es solo datos + una `vi.fn`.
  </action>
  <verify>
    <automated>npx vitest run __tests__/menu-data.test.ts && grep -c "id, name, precio_centimos, agotado" lib/menu-data.ts | grep -qx 1 && grep -q "CATALOGO_TEST" __tests__/helpers/menu-data-mock.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/menu-data.test.ts` pasa
    - `grep -n "select(" lib/menu-data.ts` muestra que la query de `getMenuItemLive` pide `id, name, precio_centimos, agotado` y ninguna otra columna
    - `getMenuItemLive` sigue siendo `export async function` y sigue SIN aparecer dentro de una llamada a `unstable_cache(`
    - `__tests__/helpers/menu-data-mock.ts` exporta `CATALOGO_TEST` con 6 entradas, exactamente una con `agotado: true` (id 1020)
    - `npx tsc --noEmit` sale con código 0
    - `npx vitest run` completo sigue en verde (ningún test preexistente rompe con este cambio)
  </acceptance_criteria>
  <done>La lectura viva devuelve el nombre y existe el catálogo de prueba compartido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /api/charge cobra el precio vivo y rechaza agotado</name>
  <files>app/api/charge/route.ts, __tests__/api-charge.precio-vivo.test.ts, __tests__/api-charge.caracterizacion.test.ts, __tests__/api-charge.alertas.test.ts, __tests__/api-charge.metadata.test.ts, __tests__/api-charge.rate-limit.test.ts, __tests__/api-charge.validacion.test.ts</files>
  <read_first>
    - app/api/charge/route.ts (archivo a modificar; leer completo — el loop de items es solo una parte, el resto no se toca)
    - __tests__/api-charge.caracterizacion.test.ts (leer completo: es la suite que define el contrato actual, incluida la antiregresión del exploit de S/3)
    - __tests__/helpers/menu-data-mock.ts (creado en la Task 1)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-PATTERNS.md (sección `app/api/charge/route.ts` — los 4 cambios exactos)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md (Code Examples → "Rechazo de agotado en el servidor"; Pitfall 1 y Pitfall 3)
  </read_first>
  <behavior>
    - Un pedido de `[{id:1001,qty:2},{id:1015,qty:1}]` cobra `amount: 3980` en el body que se manda a Culqi
    - Ese mismo pedido con `amount: 300` y `total: 3` añadidos por el cliente sigue cobrando `3980` (antiregresión del exploit)
    - Un pedido que incluye `{id:1020,qty:1}` (Lobo Sunset, agotado) responde 400 con `{ error: "Un producto de tu pedido ya no está disponible" }` y NO llama a `fetch` (Culqi nunca se toca)
    - El rechazo por agotado ocurre aunque el resto del carrito sea válido
    - Un id fuera del catálogo (9999) sigue respondiendo 400 `{ error: "Hay un producto que ya no está disponible" }`
    - `getMenuItemLive` se llama una vez por línea del pedido, en el mismo request (no hay memoización entre requests)
    - Un total que supera MAX_CENTS (`[{id:1030,qty:20},{id:1029,qty:20}]` = 84800) responde 400 sin llamar a Culqi
    - `detalle` guardado en `pedidos.items` conserva la forma `{ id, name, price, qty }` con `price` en soles
  </behavior>
  <action>
RED primero: escribir `__tests__/api-charge.precio-vivo.test.ts` (primera línea `// @vitest-environment node`)
cubriendo el bloque `<behavior>`, correrlo y confirmar que falla antes de tocar el route handler.

El test mockea `@/lib/menu-data` con el helper de la Task 1, además de los mocks que ya usa la
caracterización:
`vi.mock("@/lib/menu-data", () => menuDataMock())` no sirve tal cual porque `vi.mock` se hoistea sobre los
imports — usar el patrón de fábrica con `vi.hoisted`: declarar el mock con `const mock = vi.hoisted(() => ...)`
o, más simple y consistente con el repo, `vi.mock("@/lib/menu-data", () => ({ getMenuItemLive: vi.fn() }))` y
en `beforeEach` hacer `vi.mocked(getMenuItemLive).mockImplementation(async (id) => CATALOGO_TEST.find(i => i.id === id))`.
Elegir la segunda forma. Mantener también `vi.mock("@/lib/supabase", ...)` como hace la caracterización, porque
el route handler sigue usando Supabase para el rate limit y el insert de `pedidos`.

Cambios en `app/api/charge/route.ts` — SOLO el bloque "Total calculado en el servidor" y el import:
1. `import { getMenuItem } from "@/lib/menu";` -> `import { getMenuItemLive } from "@/lib/menu-data";`
2. `const item = getMenuItem(linea.id);` -> `const item = await getMenuItemLive(linea.id);`
3. Inmediatamente después del chequeo `if (!item)`, insertar el chequeo de agotado, devolviendo 400 con
   el texto exacto `"Un producto de tu pedido ya no está disponible"` (distinto del texto de id inexistente,
   a propósito: son dos causas distintas y el cliente debe poder distinguirlas).
4. `totalCents += Math.round(item.price * 100) * linea.qty;` -> `totalCents += item.precio_centimos * linea.qty;`
5. `detalle.push({ id: item.id, name: item.name, price: item.precio_centimos / 100, qty: linea.qty });`
   La forma de `detalle` no cambia (es lo que se guarda en `pedidos.items` y lo que lee `/admin`).
6. Actualizar el comentario de cabecera del archivo: el total ya no se recalcula "contra lib/menu.ts" sino
   contra la tabla `menu_items` leída en vivo, sin caché, en cada request. Conservar la frase que documenta
   el exploit histórico ("Antes el monto venia del cliente y se podia pagar S/3 un pedido de S/38").
Nada más del archivo se toca: rate limit, validación de email/teléfono, cargo a Culqi, insert en `pedidos`,
manejo del código `23505`, metadata del webhook y alertas quedan idénticos.

PROHIBIDO importar `getMenuItemsCached` en este archivo (es el pitfall #3 de la research: reusar la función
cacheada en la ruta de cobro reintroduce exactamente el bug que MENU-04 existe para prevenir).

Después, retrofit de las 5 suites existentes que hoy dependen del array estático
(`api-charge.caracterizacion`, `api-charge.alertas`, `api-charge.metadata`, `api-charge.rate-limit`,
`api-charge.validacion`): agregar el mismo `vi.mock("@/lib/menu-data", ...)` + `mockImplementation` en
`beforeEach`, y reemplazar los ids viejos por los del catálogo de prueba —
`id: 1` -> `id: 1001`, `id: 10` -> `id: 1015`, `id: 14` -> `id: 1030`. Ajustar las aritméticas que están
escritas a mano en los asserts y en los comentarios:
- caracterización, "recalcula el total": `[{id:1001,qty:2},{id:1015,qty:1}]` -> `amount` esperado `3980`
  (antes 4100). Actualizar también el comentario inline con los nombres/precios nuevos.
- caracterización, antiregresión del exploit: mismo pedido, mismo `3980`.
- caracterización, techo `MAX_CENTS`: el ítem más caro de la carta real es S/22.50, y 20 unidades
  (45000) ya NO superan MAX_CENTS. Cambiar el pedido a dos líneas: `[{id:1030,qty:20},{id:1029,qty:20}]`
  = 84800 céntimos. Actualizar el comentario del test para que explique ese cambio.
- caracterización, id inexistente: `999` -> `9999` (fuera del catálogo de prueba).
- El comentario sobre `MIN_CENTS` inalcanzable sigue siendo cierto (el ítem más barato de la carta real
  es S/3.50 = 350 > 300): actualizarlo con el dato nuevo en vez de borrarlo.
Cualquier otra suite que rompa al correr `npx vitest run` (por ejemplo `culqi-webhook`) se arregla con el
mismo patrón; NO se borra ningún test ni se relaja ninguna aserción para hacerla pasar.
  </action>
  <verify>
    <automated>npx vitest run __tests__/api-charge.precio-vivo.test.ts && npx vitest run && npx tsc --noEmit && grep -c "getMenuItemsCached" app/api/charge/route.ts | grep -qx 0 && grep -q "await getMenuItemLive(" app/api/charge/route.ts && grep -q "item.agotado" app/api/charge/route.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` pasa completo, sin tests skippeados nuevos ni asserts eliminados
    - `__tests__/api-charge.precio-vivo.test.ts` incluye un caso que manda `{id:1020,qty:1}` y verifica status 400, cuerpo `{ error: "Un producto de tu pedido ya no está disponible" }` y `expect(fetch).not.toHaveBeenCalled()`
    - La antiregresión del exploit (`amount: 300`, `total: 3` en el body) sigue existiendo en `api-charge.caracterizacion.test.ts` y espera `3980`
    - `grep -n "getMenuItem\b" app/api/charge/route.ts` no devuelve nada (ya no se importa la función síncrona)
    - `grep -n "getMenuItemsCached" app/api/charge/route.ts` no devuelve nada
    - `grep -n "Math.round" app/api/charge/route.ts` no devuelve ninguna línea dentro del cálculo de `totalCents`
    - `npx tsc --noEmit` sale con código 0
    - `npm run lint` no reporta ninguna regla distinta de `react-hooks/set-state-in-effect`, y sigue habiendo exactamente 3 errores de esa regla (baseline)
  </acceptance_criteria>
  <done>`/api/charge` cobra contra el precio vigente en la base y rechaza agotados, con toda la suite en verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: /api/culqi/order calcula contra el precio vivo y rechaza agotado</name>
  <files>app/api/culqi/order/route.ts, __tests__/api-culqi-order.test.ts</files>
  <read_first>
    - app/api/culqi/order/route.ts (archivo a modificar, 107 líneas, leer completo)
    - app/api/charge/route.ts (ya migrado en la Task 2 — este archivo replica el mismo cambio literalmente)
    - __tests__/helpers/menu-data-mock.ts
    - __tests__/api-charge.precio-vivo.test.ts (escrito en la Task 2 — mismo esqueleto de mocks)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-PATTERNS.md (sección `app/api/culqi/order/route.ts`)
  </read_first>
  <behavior>
    - `[{id:1001,qty:2},{id:1015,qty:1}]` produce `amount: 3980` en el body enviado a `https://api.culqi.com/v2/orders`
    - Un pedido con `{id:1020,qty:1}` responde 400 `{ error: "Un producto de tu pedido ya no está disponible" }` sin llamar a `fetch`
    - Un pedido de `[{id:1021,qty:1}]` (Agua San Luis, 350) responde 400 `{ error: "El monto del pedido no es válido" }` porque MIN_CENTS de Órdenes es 600
    - Un id fuera del catálogo responde 400 `{ error: "Hay un producto que ya no está disponible" }`
    - Sin `CULQI_SECRET_KEY` responde 500 `{ error: "Pasarela no configurada" }` (comportamiento actual, no cambia)
  </behavior>
  <action>
RED primero: `__tests__/api-culqi-order.test.ts` (primera línea `// @vitest-environment node`), mismo esqueleto
de mocks que `api-charge.precio-vivo.test.ts` pero SIN `vi.mock("@/lib/supabase", ...)` — esta ruta no toca
Supabase directamente, solo `@/lib/menu-data`. Setear `process.env.CULQI_SECRET_KEY` en `beforeEach` y
restaurarlo en `afterEach`. Mockear `fetch` global igual que hacen las suites de charge. Confirmar que falla
antes de tocar el route handler.

Aplicar en `app/api/culqi/order/route.ts` exactamente los mismos 4 cambios de la Task 2:
`import { getMenuItemLive } from "@/lib/menu-data";`, `const item = await getMenuItemLive(linea.id);`,
chequeo de `item.agotado` con el texto `"Un producto de tu pedido ya no está disponible"` justo después del
`if (!item)`, y `totalCents += item.precio_centimos * linea.qty;`. Esta ruta no arma `detalle`, así que no hay
un quinto cambio. Actualizar el comentario de cabecera: el monto ya no se recalcula "contra lib/menu.ts" sino
contra la tabla `menu_items` leída en vivo, con la misma regla que `/api/charge`.

Agregar en el comentario de cabecera una línea explícita advirtiendo que este archivo y
`app/api/charge/route.ts` duplican el cálculo del total a propósito y deben mantenerse en sync a mano — no hay
abstracción compartida, y es el pitfall #3 documentado en 02-RESEARCH.md.

No tocar nada más: `ordenUnica()`, `client_details`, `expiration_date`, el manejo del 502 y las constantes
`MIN_CENTS = 600` / `MAX_CENTS` / `MAX_QTY` quedan idénticos.
  </action>
  <verify>
    <automated>npx vitest run __tests__/api-culqi-order.test.ts && npx vitest run && npx tsc --noEmit && grep -q "await getMenuItemLive(" app/api/culqi/order/route.ts && grep -c "getMenuItem\b\|getMenuItemsCached" app/api/culqi/order/route.ts | grep -qx 0 && npm run lint 2>&1 | grep -E "^\s+[0-9]+:[0-9]+\s+error" | grep -v "react-hooks/set-state-in-effect" | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/api-culqi-order.test.ts` cubre los 5 comportamientos del bloque `<behavior>`
    - `npx vitest run` completo en verde
    - `npx tsc --noEmit` sale con código 0
    - `grep -rn "from \"@/lib/menu\"" app/api/` no devuelve ninguna línea (ninguna ruta de API importa ya el módulo de tipos para obtener precios)
    - `npm run lint` reporta exactamente 3 errores, todos `react-hooks/set-state-in-effect` (paridad con la baseline del merge-base)
    - El comentario de cabecera de ambas rutas de cobro menciona explícitamente que el cálculo está duplicado y debe mantenerse en sync
  </acceptance_criteria>
  <done>Las dos rutas de cobro leen el precio vivo de Supabase y rechazan agotados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `POST /api/charge` | El cliente manda `{id, qty}`; todo lo demás que mande sobre precio es ruido y se ignora |
| navegador → `POST /api/culqi/order` | Igual: el monto de la Orden lo decide el servidor |
| app (service_role) → Postgres | Lectura viva de `menu_items` en cada request de cobro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Tampering | Cliente manda `amount`/`total` en el body de `/api/charge` | mitigate | El servidor sigue recalculando; el test de antiregresión del exploit de S/3 se mantiene en `api-charge.caracterizacion.test.ts` con el monto nuevo (3980) |
| T-02-07 | Tampering | Request armado a mano con un producto `agotado` (bypass del botón deshabilitado) | mitigate | Chequeo explícito `if (item.agotado) return 400` en las DOS rutas, antes de cualquier llamada a Culqi; cubierto por test en cada suite |
| T-02-08 | Tampering | Alguien reusa `getMenuItemsCached` en la ruta de cobro "por DRY" y cobra un precio stale | mitigate | Gate de grep en el `<verify>` de las Tasks 2 y 3: `getMenuItemsCached` no puede aparecer en ninguna de las dos rutas de cobro |
| T-02-09 | Tampering | Error de redondeo de punto flotante al convertir soles→céntimos con precios decimales reales (S/17.90) | mitigate | Aritmética entera pura sobre `precio_centimos`; se elimina `Math.round(item.price * 100)` del cálculo del total |
| T-02-10 | Denial of Service | Una lectura viva por línea del pedido agrega latencia a un request que ya llama a Culqi | accept | Máximo ~20 líneas por pedido, negocio de barrio, la lectura es por PK sobre una tabla de 30 filas. El costo es el precio de MENU-04 y es deliberado |
</threat_model>

<verification>
1. `npx tsc --noEmit` sale con código 0
2. `npx vitest run` completo en verde, incluidas las dos suites nuevas
3. `npm run lint` en paridad con la baseline: exactamente 3 errores, todos `react-hooks/set-state-in-effect`
4. `grep -rn "getMenuItemsCached" app/api/charge/route.ts app/api/culqi/order/route.ts` vacío
5. `grep -rn "from \"@/lib/menu\"" app/api/` vacío
6. El árbol compila: `npm run build` termina sin error (`lib/menu.ts` conserva `MENU_ITEMS`/`getMenuItem` para `app/page.tsx`, que migra en el plan 02-04)
</verification>

<success_criteria>
- `POST /api/charge` y `POST /api/culqi/order` calculan el total con `await getMenuItemLive(id)` en cada request
- Ambas rechazan con 400 un pedido que incluya un producto `agotado`, antes de tocar Culqi
- La aritmética del total es entera sobre `precio_centimos`
- La batería de tests de cobro corre contra el catálogo de prueba con ids >= 1001 y sigue cubriendo la antiregresión del exploit histórico
- Ningún commit de este plan deja el árbol sin compilar
</success_criteria>

<output>
Create `.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-02-SUMMARY.md` when done
</output>
