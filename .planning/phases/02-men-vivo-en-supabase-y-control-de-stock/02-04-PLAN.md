---
phase: 02-men-vivo-en-supabase-y-control-de-stock
plan: 04
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - app/api/menu/route.ts
  - __tests__/api-menu.test.ts
  - lib/menu.ts
  - lib/menu-data.ts
  - app/page.tsx
  - components/promo-slider.tsx
  - __tests__/menu.test.ts
  - public/images/menu/a.webp
  - public/images/menu/b.webp
  - public/images/menu/burgerpollodesi.webp
  - public/images/menu/doublecarne.webp
  - public/images/menu/miami-night.webp
  - public/images/menu/tropical.webp
autonomous: true
requirements: [MENU-01, MENU-02, MENU-03]

must_haves:
  truths:
    - "La carta pública muestra la carta real de Jaime leída de la tabla menu_items, no del array de lib/menu.ts"
    - "La carta pública se sirve de la caché de Next: visitas repetidas no golpean Supabase"
    - "Los chips de categoría son las seis categorías reales de la carta física, en el orden del documento fuente"
    - "Un producto sin foto muestra el ícono de su categoría, no un cuadro vacío ni el texto [Foto: ...]"
    - "El carrusel de promos ya no agrega al carrito ids que no existen en la base"
    - "Los precios con decimales se muestran con dos decimales (S/17.90, no S/17.9)"
    - "No queda ninguna imagen huérfana de los productos inventados en /public/images/menu/"
  artifacts:
    - path: "app/api/menu/route.ts"
      provides: "Endpoint público del menú, servido desde la caché con tag `menu`"
      exports: ["GET"]
    - path: "lib/menu.ts"
      provides: "Tipos puros y CATEGORIES reales, importable desde \"use client\""
      exports: ["MenuItem", "CATEGORIES"]
    - path: "app/page.tsx"
      provides: "Carta pública consumiendo /api/menu con estado de carga e íconos por categoría"
  key_links:
    - from: "app/page.tsx"
      to: "/api/menu"
      via: "fetch dentro de useEffect, sin polling"
      pattern: "fetch\\(\"/api/menu\"\\)"
    - from: "app/api/menu/route.ts"
      to: "getMenuItemsCached"
      via: "import desde @/lib/menu-data"
      pattern: "getMenuItemsCached\\(\\)"
    - from: "app/page.tsx"
      to: "CATEGORIES"
      via: "import estático desde @/lib/menu (constante, no viene de la DB)"
      pattern: "CATEGORIES"
---

<objective>
Cerrar el circuito de lectura pública: crear `GET /api/menu` sobre la función cacheada, y migrar
`app/page.tsx` del array estático a ese endpoint, con las seis categorías reales, íconos de
categoría como placeholder y los precios de la carta real bien formateados. En el mismo movimiento
se eliminan `MENU_ITEMS`/`getMenuItem` de `lib/menu.ts` y las imágenes huérfanas de los productos
inventados.

Purpose: MENU-01 y MENU-02. Es el momento en que el visitante deja de ver "Miami Night" y pasa a
ver la carta real de Jaime.

Output: la home sirviendo la carta desde la base, cacheada; `lib/menu.ts` reducido a tipos y
constantes; el repo sin datos de productos inventados.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-CONTEXT.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-PATTERNS.md
@.planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-UI-SPEC.md

<interfaces>
<!-- Contratos reales del repo. No explorar el codebase para esto. -->

lib/menu-data.ts (plan 02-01, modificado por 02-02):
  export const MENU_CATEGORIES: readonly string[]   // se ELIMINA en este plan (se muda a lib/menu.ts)
  export const getMenuItemsCached: () => Promise<MenuItem[]>
  export async function getMenuItemLive(id): Promise<{ id, name, precio_centimos, agotado } | undefined>
  export async function updateMenuItem(id, patch): Promise<void>

lib/menu.ts (estado ACTUAL, a reducir):
  export type MenuItem = { id, category, name, description, price, badge, originalPrice, image, agotado }
  export const CATEGORIES = ["Combos", "Burgers", "Pollo", "Complementos", "Bebidas"]   // se REEMPLAZA
  export const MENU_ITEMS: MenuItem[]                    // se ELIMINA
  const BY_ID = new Map(...)                             // se ELIMINA
  export function getMenuItem(id): MenuItem | undefined  // se ELIMINA

lib/utils.ts:
  export function formatPrice(price: number): string   // devuelve `S/${price.toFixed(2)}`

app/page.tsx (estado ACTUAL, puntos de anclaje):
  línea 12: import { MENU_ITEMS as menuItems, CATEGORIES as categories, type MenuItem } from "@/lib/menu";
  líneas 22-27: const heroPromos: HeroPromo[] = [ ... ids 13, 14, 2, 6 hardcodeados ... ]
  líneas 29-38: function placeholderColor(category) — switch sobre las 5 categorías viejas
  líneas 85-107: rama `item.image ? <Image/> : <span>[Foto: {item.name}]</span>` dentro de MenuCard
  línea 119: <span ...>S/{item.price}</span>   y línea 121: S/{item.originalPrice}
  línea 162: const [active, setActive] = useState("Combos");
  línea 165: const filtered = menuItems.filter(i => i.category === active);

components/promo-slider.tsx:
  export type HeroPromo = { id: number; name: string; price: number; tag?: string; placeholder: string; image?: string }
  PromoSlide llama add({ id: promo.id, name: promo.name, price: promo.price })  // agrega AL CARRITO
  línea ~80: <p className="font-mono text-xl font-bold mt-1" ...>S/{promo.price}</p>

Íconos verificados contra node_modules/lucide-react@1.22.0 (existen los 6):
  CupSoda, Sandwich, Hamburger, Drumstick, ShoppingBasket, Package
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GET /api/menu sobre la función cacheada</name>
  <files>app/api/menu/route.ts, __tests__/api-menu.test.ts</files>
  <read_first>
    - app/api/admin/pedidos/route.ts (analog del GET: try/catch + console.error + 500 genérico)
    - lib/menu-data.ts (firma de `getMenuItemsCached`)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-RESEARCH.md (Pattern 3 — esta ruta NO lleva `export const dynamic`)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-UI-SPEC.md (Copywriting → carta pública: el mensaje de error que muestra el cliente)
  </read_first>
  <behavior>
    - `GET /api/menu` responde 200 con `{ items: MenuItem[] }` tal como los devuelve `getMenuItemsCached()`
    - `GET /api/menu` responde 500 `{ error: "No se pudo cargar la carta" }` si la lectura lanza
    - La ruta llama a `getMenuItemsCached` exactamente una vez por request
    - La ruta NUNCA importa `getMenuItemLive` ni `updateMenuItem`
  </behavior>
  <action>
RED primero: `__tests__/api-menu.test.ts` con `// @vitest-environment node` en la primera línea, mockeando
`vi.mock("@/lib/menu-data", () => ({ getMenuItemsCached: vi.fn() }))` (con eso no se carga `server-only` ni
`next/cache`). Correrlo y confirmar el rojo antes de crear la ruta.

Crear `app/api/menu/route.ts`. Solo exporta `GET`. Llama `await getMenuItemsCached()` y devuelve
`Response.json({ items })`. try/catch con `console.error("No se pudo cargar la carta:", e)` y
`Response.json({ error: "No se pudo cargar la carta" }, { status: 500 })`.

NO agregar `export const dynamic`, `export const revalidate` ni ninguna route segment config: los Route
Handlers de Next 16 no se cachean por defecto, y la caché de este endpoint vive DENTRO de
`getMenuItemsCached` (tag `menu`), que es lo que permite invalidarla desde `/api/admin/menu`. Marcar la ruta
como estática congelaría la respuesta y rompería MENU-03.

Comentario de cabecera explicando: endpoint público (no lleva auth, es la carta), la caché la maneja
`getMenuItemsCached` con el tag `menu`, y por eso un `PATCH /api/admin/menu` se ve acá sin deploy.
  </action>
  <verify>
    <automated>npx vitest run __tests__/api-menu.test.ts && npx tsc --noEmit && grep -c "export const dynamic\|export const revalidate\|getMenuItemLive\|updateMenuItem" app/api/menu/route.ts | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run __tests__/api-menu.test.ts` cubre los 4 comportamientos del bloque `<behavior>`
    - `app/api/menu/route.ts` exporta únicamente `GET`
    - `grep -n "dynamic\|revalidate" app/api/menu/route.ts` no devuelve ninguna línea de código (solo comentarios, si acaso)
    - `npx tsc --noEmit` sale con código 0
  </acceptance_criteria>
  <done>El endpoint público del menú existe y está testeado.</done>
</task>

<task type="auto">
  <name>Task 2: la home lee de /api/menu y se retira el array estático (commit atómico)</name>
  <files>lib/menu.ts, lib/menu-data.ts, app/page.tsx, components/promo-slider.tsx, __tests__/menu.test.ts</files>
  <read_first>
    - lib/menu.ts (archivo a reducir — leer completo, incluido el comentario de cabecera)
    - lib/menu-data.ts (de acá sale `MENU_CATEGORIES`, que se muda)
    - app/page.tsx (archivo a modificar — leer completo, 260 líneas)
    - components/promo-slider.tsx (leer `PromoSlide`: el botón "Agregar" del carrusel mete el id del promo directamente al carrito)
    - app/admin/page.tsx líneas 264-300 (patrón `refresh` + `useEffect` + `useState` de fetch client-side de este repo)
    - lib/utils.ts (`formatPrice`)
    - __tests__/menu.test.ts (archivo a borrar — confirmar que solo prueba `getMenuItem`/`MENU_ITEMS`)
    - AGENTS.md (guardrails 1 y 3: compilación atómica y directiva `"use client"` como primera sentencia)
  </read_first>
  <action>
GUARDRAIL 1 — COMPILACIÓN ATÓMICA: este es un único commit. Borrar `MENU_ITEMS`/`getMenuItem` y migrar
`app/page.tsx` van juntos, porque `app/page.tsx` es el último consumidor vivo. Antes de borrar, correr y
pegar la salida de:
`grep -rn --include='*.ts' --include='*.tsx' 'MENU_ITEMS\|getMenuItem\b\|MENU_CATEGORIES' app components lib __tests__`
Si aparece algún consumidor fuera de los archivos de esta task, migrarlo en el mismo commit o parar y reportar.

`lib/menu.ts`:
- Reemplazar `CATEGORIES` por las seis categorías reales, en el orden del documento fuente (Claude's Discretion
  ya documentada en 02-CONTEXT.md): "Enchiladas", "Broaster", "Salchipapas / Power Plates", "Combos xtremos",
  "Bebidas", "Hamburguesas". Los strings deben ser IDÉNTICOS a los valores de `category` sembrados en
  `supabase/migrations/20260901000000_menu_items.sql` — verificarlos contra el archivo de migración, no de memoria.
- Borrar `MENU_ITEMS`, `BY_ID` y `getMenuItem`.
- Conservar `type MenuItem` tal cual (ya incluye `agotado`).
- Reescribir el comentario de cabecera: este archivo ya NO es la fuente de verdad del precio — la fuente es la
  tabla `menu_items`; acá solo viven el tipo compartido y el orden de categorías de la UI, y por eso sigue
  siendo seguro importarlo desde `"use client"` (no tiene ningún import de Supabase). Mencionar que el precio
  que se cobra sale de `getMenuItemLive` en `lib/menu-data.ts`.

`lib/menu-data.ts`:
- Borrar `MENU_CATEGORIES` (se mudó a `lib/menu.ts` como `CATEGORIES`; era inalcanzable desde el cliente por
  estar en un módulo `server-only`, que es exactamente el problema que este cambio resuelve). Si algo lo
  importaba, migrarlo a `CATEGORIES` de `@/lib/menu` en este mismo commit.
- No tocar nada más del archivo.

`app/page.tsx` — `"use client"` sigue siendo la PRIMERA sentencia del archivo, sin ningún import por encima
(guardrail 3: un import arriba de la directiva tira todas las rutas del sitio):
- Reemplazar el import de la línea 12 por `import { CATEGORIES as categories, type MenuItem } from "@/lib/menu";`
  y agregar `import { formatPrice } from "@/lib/utils";`.
- En `HomePage`: `const [menuItems, setMenuItems] = useState<MenuItem[]>([])` y
  `const [loading, setLoading] = useState(true)`. Un `useEffect` con dependencias `[]` que hace
  `fetch("/api/menu")`, `.then(r => r.json())`, `.then(({ items }) => setMenuItems(items ?? []))`,
  `.catch(() => setMenuItems([]))`, `.finally(() => setLoading(false))`.
  REGLA DURA DE LINT: ningún `setState` puede ejecutarse de forma síncrona en el cuerpo del efecto — todos van
  dentro de los callbacks de la promesa. La baseline de `npm run lint` tiene exactamente 3 errores
  `react-hooks/set-state-in-effect` y esta task no puede agregar un cuarto.
- `const [active, setActive] = useState(categories[0]);` (ya no el literal "Combos").
- `placeholderColor`: reemplazar el switch por las seis categorías reales, reusando los tonos ya definidos en
  el archivo para no introducir colores nuevos: "Enchiladas" -> ACCENT_SOFT, "Hamburguesas" -> PRIMARY_SOFT,
  "Broaster" -> "#FFE8D1", "Salchipapas / Power Plates" -> "#FFF3C4", "Combos xtremos" -> ACCENT_SOFT,
  "Bebidas" -> "#DDF0EE", default -> PRIMARY_SOFT.
- `heroPromos`: dejar de ser una constante de módulo con ids inventados (13, 14, 2, 6 ya no existen en la base;
  el carrusel los mete al carrito y el checkout los rechazaría con "Hay un producto que ya no está disponible").
  Derivarlo de los items ya cargados, con `useMemo` sobre `menuItems`: tomar los primeros 4 items que cumplan
  `item.image !== null && !item.agotado`, mapeados a
  `{ id, name, price, tag: item.badge ?? undefined, placeholder: \`Foto: ${item.name}\`, image: item.image }`.
  Renderizar `<PromoSlider promos={heroPromos} />` SOLO si `heroPromos.length > 0` (con la semilla actual hay 7
  productos con foto, así que siempre habrá 4; el guard evita que el carrusel se rompa con una lista vacía
  mientras carga).
- Precios: `S/{item.price}` -> `{formatPrice(item.price)}` y `S/{item.originalPrice}` ->
  `{formatPrice(item.originalPrice)}` (la carta real tiene precios con decimales: S/17.90 se veía "S/17.9").
- Estado de carga: mientras `loading` es true, en el grid de productos renderizar 6 tiles vacíos con
  `background: placeholderColor(active)` y el mismo `paddingTop: "62%"` de `MenuCard`, para no producir un
  salto de layout. Si `!loading && filtered.length === 0`, mostrar el copy exacto de 02-UI-SPEC.md:
  "Todavía no hay productos en esta categoría."
- Si el fetch falló (lista vacía y `!loading`) mostrar, encima del grid, el copy exacto de 02-UI-SPEC.md:
  "No pudimos cargar la carta. Actualiza la página o intenta en unos segundos."
- El resto del archivo (hero, CheeseDrip, LocalReel, footer, `MenuCard` salvo los precios) no se toca.

`components/promo-slider.tsx`: cambiar `S/{promo.price}` por `{formatPrice(promo.price)}`, agregando
`import { formatPrice } from "@/lib/utils";` DEBAJO de la directiva `"use client"`, nunca encima.

Borrar `__tests__/menu.test.ts` (probaba `getMenuItem(1)` -> "Miami Night", símbolos que dejan de existir;
`__tests__/menu-data.test.ts` ya cubre la capa de datos que lo reemplaza).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build && test ! -f __tests__/menu.test.ts && grep -c "MENU_ITEMS\|getMenuItem\b" lib/menu.ts | grep -qx 0 && grep -q "fetch(\"/api/menu\")" app/page.tsx && head -1 app/page.tsx | grep -qx '"use client";' && npm run lint 2>&1 | grep -E "^\s+[0-9]+:[0-9]+\s+error" | grep -v "react-hooks/set-state-in-effect" | wc -l | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` termina sin error (el árbol compila en este commit, no en el siguiente)
    - `grep -rn 'MENU_ITEMS\|getMenuItem\b\|MENU_CATEGORIES' app components lib __tests__` no devuelve ninguna línea de código
    - `CATEGORIES` en `lib/menu.ts` tiene los 6 strings y son idénticos, carácter por carácter, a los valores distintos de `category` del seed de `supabase/migrations/20260901000000_menu_items.sql`
    - La primera línea de `app/page.tsx` y de `components/promo-slider.tsx` sigue siendo `"use client";`
    - `grep -n "heroPromos" app/page.tsx` muestra que se deriva de `menuItems` con `useMemo`, y no quedan ids literales (13, 14, 2, 6) en el archivo
    - `npm run lint` reporta exactamente 3 errores, todos `react-hooks/set-state-in-effect`, en `app/puntos/page.tsx` y `lib/cart-context.tsx` — ninguno en `app/page.tsx`
    - `npx vitest run` completo en verde
    - Los tres strings de copy de 02-UI-SPEC.md aparecen literalmente en `app/page.tsx`: "No pudimos cargar la carta. Actualiza la página o intenta en unos segundos." y "Todavía no hay productos en esta categoría."
  </acceptance_criteria>
  <done>La home muestra la carta real leída de la base, y el array estático ya no existe en ningún lado.</done>
</task>

<task type="auto">
  <name>Task 3: íconos de categoría como placeholder y limpieza de imágenes huérfanas</name>
  <files>app/page.tsx, public/images/menu/a.webp, public/images/menu/b.webp, public/images/menu/burgerpollodesi.webp, public/images/menu/doublecarne.webp, public/images/menu/miami-night.webp, public/images/menu/tropical.webp</files>
  <read_first>
    - app/page.tsx (el bloque `item.image ? <Image/> : <span>[Foto: ...]</span>` dentro de `MenuCard`)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-UI-SPEC.md (secciones "Icon Mapping — Placeholder por Categoría" y "Loading state de la carta pública" — contrato visual completo)
    - .planning/phases/02-men-vivo-en-supabase-y-control-de-stock/02-CONTEXT.md (sección `### Imágenes / placeholders`: el reemplazo ícono→foto debe poder hacerse producto por producto)
    - supabase/migrations/20260901000000_menu_items.sql (las 7 rutas de imagen sembradas — son las únicas que se conservan)
  </read_first>
  <action>
En `app/page.tsx`, agregar un mapa de íconos a nivel de módulo (no dentro del componente):
`const CATEGORY_ICON: Record<string, React.ElementType>` con el mapeo ya resuelto y verificado contra
`lucide-react@1.22.0` en 02-UI-SPEC.md: "Bebidas" -> `CupSoda`, "Enchiladas" -> `Sandwich`,
"Hamburguesas" -> `Hamburger`, "Broaster" -> `Drumstick`, "Salchipapas / Power Plates" -> `ShoppingBasket`,
"Combos xtremos" -> `Package`. Fallback a `Package` para una categoría desconocida. Agregar los 6 íconos al
import existente de `lucide-react` (que ya trae Plus, Minus, Check, Phone, Clock, Bike, MapPin, BookText).

Reemplazar la rama `else` del `item.image ? ... : ...` de `MenuCard`: en vez del `<span>[Foto: {item.name}]</span>`,
renderizar el ícono de la categoría centrado con `absolute inset-0 flex items-center justify-center`,
`size={32}` y `style={{ color: "rgba(36,31,28,0.35)" }}` — mismo tono que el texto que reemplaza, para que un
placeholder no tenga más contraste que una foto real. El contenedor `paddingTop: "62%"` y el
`background: placeholderColor(item.category)` no cambian: el aspect ratio del tile es el mismo con foto y sin
foto. Dejar un comentario de una línea explicando que este es el mecanismo del reemplazo incremental: en cuanto
un producto tenga `image` seteado en la tabla (un solo UPDATE de esa columna), el ícono desaparece solo, sin
tocar `MenuCard` ni el resto de los productos.

En el estado de carga introducido en la Task 2, pintar dentro de cada tile el ícono de la categoría activa a
opacidad reducida (`rgba(36,31,28,0.15)`), en vez de dejar el tile de color plano — reutiliza el vocabulario
visual del placeholder en vez de introducir un skeleton gris distinto.

Limpieza de imágenes huérfanas. ANTES de borrar, correr y pegar la salida de:
`grep -rn --include='*.ts' --include='*.tsx' --include='*.sql' --include='*.md' 'a\.webp\|b\.webp\|burgerpollodesi\|doublecarne\|miami-night\|tropical\.webp' app components lib supabase public`
Solo si ninguna referencia viva aparece, borrar con `git rm` estos 6 archivos, que pertenecen a los productos
inventados que 02-CONTEXT.md manda eliminar (no reciclar):
`public/images/menu/a.webp`, `b.webp`, `burgerpollodesi.webp`, `doublecarne.webp`, `miami-night.webp`,
`tropical.webp`.
NO borrar los 7 que sí usa el seed: `baconcheese.webp`, `burgazo.webp`, `choriroyal.webp`,
`el-breakfast-del-lobo.webp`, `filete-de-pollo-royal.webp`, `salchipobre.webp`, `tropical-burger.webp`
(ojo: `tropical.webp` y `tropical-burger.webp` son archivos distintos — solo se borra el primero).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npm run build && ls public/images/menu/ | wc -l | grep -qx 7 && grep -c "CupSoda\|Drumstick\|ShoppingBasket" app/page.tsx | grep -qv "^0$" && grep -c "\[Foto:" app/page.tsx | grep -qx 0 && npm run lint 2>&1 | grep -E "^\s+[0-9]+:[0-9]+\s+error" | grep -v "react-hooks/set-state-in-effect" | wc -l | grep -qx 0 && echo OK