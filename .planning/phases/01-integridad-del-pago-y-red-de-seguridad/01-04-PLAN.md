---
phase: 01-integridad-del-pago-y-red-de-seguridad
plan: 04
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/orders-store.ts
  - lib/cart-context.tsx
  - app/admin/page.tsx
  - .context/DEPLOY.md
  - __tests__/orders-store.test.ts
autonomous: true
requirements: [CLEAN-02, CLEAN-03, CLEAN-04]
user_setup: []

must_haves:
  truths:
    - "Un checkout real ya no escribe una copia muerta del pedido en localStorage"
    - "La pantalla de confirmacion y el link de WhatsApp siguen funcionando exactamente igual"
    - "El carrito sigue sobreviviendo a un refresh (vive en otra clave y otro archivo)"
    - "El panel /admin ya no tiene botones que dicen hacer algo que no hacen"
    - ".context/DEPLOY.md describe el estado real del sistema, no el de hace dos meses"
  artifacts:
    - path: "lib/orders-store.ts"
      provides: "Tipos Order/OrderStatus y un constructor puro del objeto de confirmacion, sin persistencia"
      exports: ["Order", "OrderStatus"]
    - path: "__tests__/orders-store.test.ts"
      provides: "Fija la forma del objeto de confirmacion ANTES de quitar la escritura a localStorage"
  key_links:
    - from: "lib/cart-context.tsx"
      to: "lib/orders-store.ts"
      via: "submitOrder construye el Order local sin persistirlo"
      pattern: "from \"@/lib/orders-store\""
    - from: "lib/cart-context.tsx"
      to: "buildWhatsAppUrl"
      via: "el objeto devuelto por submitOrder tiene que seguir sirviendo de entrada"
      pattern: "buildWhatsAppUrl"
---

<objective>
Sacar del camino tres cosas que hoy mienten sobre lo que el sistema hace.

1. **CLEAN-02** — `lib/orders-store.ts` escribe una copia de cada pedido real en
   `localStorage['lobo_orders']` (`lib/cart-context.tsx:104`) que nadie vuelve a leer jamas: el
   panel lee de Supabase desde el commit `eb9f243`. Lo unico vivo de ese helper es la FORMA del
   objeto que arma para la pantalla de confirmacion y para el link de WhatsApp.
2. **CLEAN-03** — el panel tiene dos botones "Agregar pedido de prueba" (`app/admin/page.tsx:492-497`
   y `550-555`) que siembran pedidos falsos en una clave de localStorage que el panel ya no lee:
   el boton no hace visiblemente nada y confunde a quien lo aprieta. Y `ValidarTab` llama a
   `updateOrderStatus(orderId, "entregado")` (`app/admin/page.tsx:95`), que escribe en esa misma
   clave muerta en vez de en `pedidos.estado` — el "marcar entregado" del flujo de puntos no
   marca nada.
3. **CLEAN-04** — `.context/DEPLOY.md` todavia describe "pedidos en localStorage" como limitacion
   vigente (item 4 del checklist) y da por buena una nota sobre Culqi de agosto que
   `CONCERNS.md#12` ya declaro no confiable.

Purpose: higiene con consecuencias reales. Un panel con botones inertes y un doc que describe un
sistema que ya no existe hacen que la proxima persona (o el proximo agente) tome decisiones sobre
una realidad falsa.
Output: `orders-store` sin persistencia, panel sin botones inertes, DEPLOY.md al dia.

**Punto de despliegue seguro:** `lib/cart-context.tsx:104` corre en CADA checkout real. Por eso el
orden de este plan es innegociable: primero se fija en un test la forma exacta del objeto que
`submitOrder` devuelve hoy (porque de esa forma dependen la pantalla de confirmacion y
`buildWhatsAppUrl`, que es el respaldo operativo cuando el insert falla), y recien despues se quita
la escritura. Sin ese test, un campo de menos en el objeto rompe el link de WhatsApp justo en el
caso donde es lo unico que salva el pedido.

Este plan corre en paralelo con el 01-03: no comparte ni un archivo con el.
</objective>

<phase_goal>
Objetivo de fase (verbatim de `.planning/ROADMAP.md` §Phase 1):

> Un pago confirmado por Culqi siempre se convierte en un pedido registrado en Supabase, con
> alertas si algo falla, y el servidor resiste intentos de abuso o de manipular precio y envio.
</phase_goal>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
Mapa completo de consumidores de `lib/orders-store.ts` (verificado con grep sobre `app/`, `lib/`
y `components/` — esta es la lista COMPLETA, no hace falta explorar):

- `app/admin/page.tsx:4` -> `import { updateOrderStatus, saveOrder }` (ambos se eliminan)
- `app/admin/page.tsx:5` -> `import type { Order, OrderStatus }` (SE QUEDA, el panel lo usa para tipar la lista que viene de Supabase)
- `lib/cart-context.tsx:4` -> `import { saveOrder }` (se reemplaza)
- `lib/cart-context.tsx:5` -> `import type { Order }` (SE QUEDA)
- `app/checkout/page.tsx:10` -> `import type { Order }` (SE QUEDA — este archivo NO se toca)
- `getOrders()` solo se usa DENTRO de `lib/orders-store.ts` (lineas 24 y 39). Al eliminar la
  persistencia queda completamente muerto y se borra.

Firma actual de `saveOrder`, que es lo que hay que preservar en forma:
`export function saveOrder(order: Omit<Order, "id" | "createdAt" | "status">): Order`
Agrega `id: "LB-" + Date.now()`, `createdAt: new Date().toISOString()`, `status: "pendiente"`,
y escribe en `localStorage["lobo_orders"]`.

Tipo `Order` (`lib/orders-store.ts:3-15`):
`{ id, createdAt, name, phone, email?, culqiChargeId?, delivery, address, items: {id,name,price,qty}[], total, status }`

`submitOrder` (`lib/cart-context.tsx:103-112`) recibe
`{ name, phone, email?, culqiChargeId?, delivery, address }`, le suma `items` (mapeados del carrito)
y `total`, llama a `saveOrder`, limpia el carrito y cierra el drawer. Devuelve el `Order`.

Consumidores del objeto devuelto:
- `app/checkout/page.tsx:94` hace `setConfirmedOrder({ ...order, id: result.codigo })` — el `id`
  local se DESCARTA y se pisa con el `codigo` del servidor. Verificado en la discusion de contexto:
  no hay bug de identidad ahi. El unico valor vivo del helper es la forma del objeto.
- `buildWhatsAppUrl(order)` (`lib/cart-context.tsx:37-42`) lee `order.items` (con `qty`, `name`,
  `price`), `order.delivery`, `order.address`, `order.id`, `order.name`, `order.phone`, `order.total`.
  Ese es el respaldo operativo documentado cuando el insert falla — no puede romperse.

CLAVE IMPORTANTE, verificada en la discusion de contexto: la supervivencia del carrito al refrescar
NO depende de este archivo. Vive en `lib/cart-context.tsx:44` bajo la clave `lobo_cart`, otra clave
y otro archivo. Este plan no la toca.

`generateMockOrder()` (`app/admin/page.tsx:39-49`) y las constantes `MOCK_NAMES` (linea 32) y
`MOCK_ITEMS` (lineas 33-37) existen solo para alimentar los botones que se eliminan.

Convenciones: comentarios en espanol sin tildes explicando el porque. `app/admin/page.tsx` usa
alineacion en columnas para datos literales paralelos (`statusConfig` lineas 12-16, los `useState`
agrupados lineas 64-70) — al borrar lineas hay que respetar esa alineacion, no reformatearla.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Fijar la forma del objeto de confirmacion, despues quitar la persistencia (RED -> GREEN -> REFACTOR)</name>
  <read_first>
    - lib/orders-store.ts (LEER COMPLETO — 41 lineas, es el archivo que se reescribe)
    - lib/cart-context.tsx:1-5,37-42,103-112 (el consumidor vivo, buildWhatsAppUrl, y submitOrder)
    - app/checkout/page.tsx:85-141 (como se usa el objeto devuelto: la confirmacion y el href de WhatsApp)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md (D-20 — que se conserva y que se elimina)
    - .planning/codebase/CONCERNS.md (item 7 — el analisis linea por linea de este hueco)
  </read_first>
  <behavior>
    Secuencia obligatoria, en este orden:

    **Paso 1 (RED-ish, gate VERDE):** escribir `__tests__/orders-store.test.ts` contra el
    comportamiento ACTUAL de `saveOrder` y de `buildWhatsAppUrl`, y verlo pasar SIN tocar codigo.
    Esto es caracterizacion, igual que en el plan 01-01: se esta por refactorizar codigo que corre
    en cada checkout real.
      - `saveOrder({name, phone, email, culqiChargeId, delivery, address, items, total})` devuelve
        un objeto con TODOS los campos de entrada preservados, mas `id` que empieza con `"LB-"`,
        `createdAt` que parsea como fecha ISO valida, y `status` igual a `"pendiente"`.
      - `buildWhatsAppUrl(order)` produce una URL que empieza con
        `https://wa.me/51974983862?text=` y cuyo texto decodificado contiene: el `id`, el nombre,
        el telefono, una linea por item con cantidad, nombre y subtotal, y el total.
      - Con `delivery: true` el texto contiene `"Delivery a: "` y la direccion; con
        `delivery: false` contiene `"Para recoger"`.
      Estos tests corren en entorno jsdom (el de por defecto), que provee `localStorage`.

    **Paso 2 (RED real):** cambiar los tests para que exijan el comportamiento nuevo — que la
    construccion del objeto NO escriba en `localStorage`. Verlos fallar.
      - Espiar `localStorage.setItem` con `vi.spyOn` y afirmar que NO fue llamado con la clave
        `"lobo_orders"`.
      - Afirmar que el objeto devuelto tiene exactamente la misma forma que en el Paso 1. Los
        asserts del Paso 1 se conservan intactos: son la garantia de que la confirmacion y el
        WhatsApp no cambian.
      - Afirmar que `lobo_cart` (la clave del carrito) no se ve afectada por nada de este cambio.

    **Paso 3 (GREEN):** implementar y ver todo en verde.

    Anti-test: no escribir tests sobre `getOrders` ni sobre `updateOrderStatus`. Se eliminan; un
    test sobre codigo que se borra es trabajo tirado.
  </behavior>
  <action>
    Reescribir `lib/orders-store.ts` para que quede con:
    - Los tipos `OrderStatus` y `Order`, sin cambios. Los importan `app/admin/page.tsx`,
      `lib/cart-context.tsx` y `app/checkout/page.tsx`; tocarlos rompe tres archivos por nada.
    - Una funcion pura que arma el objeto de la confirmacion, con la MISMA firma y el MISMO valor
      de retorno que `saveOrder` hoy, pero sin la linea de `localStorage.setItem`. El nombre queda
      a discrecion (D — Claude's Discretion) pero tiene que decir lo que hace: no persiste, arma un
      objeto de vista. `construirOrderLocal` es una opcion razonable. Lo que NO puede seguir
      llamandose es `saveOrder`: el nombre promete una persistencia que ya no ocurre, y ese es
      exactamente el tipo de mentira que este plan viene a borrar.
    - Sin `getOrders`. Confirmado por grep que solo se usaba desde dentro de este mismo archivo
      (lineas 24 y 39); al quitar la persistencia queda muerto.
    - Sin `updateOrderStatus`. Su unico llamador (`app/admin/page.tsx:95`) se elimina en la Task 2.
    - Un comentario de cabecera en espanol explicando por que este archivo ya no persiste: los
      pedidos reales viven en Supabase desde `eb9f243`, el panel los lee de `/api/admin/pedidos`,
      y lo unico que sigue vivo aca es la forma del objeto que la pantalla de confirmacion y
      `buildWhatsAppUrl` necesitan. Sin ese comentario, alguien va a "arreglar" la falta de
      persistencia dentro de seis meses.

    Actualizar `lib/cart-context.tsx`: cambiar el import y la llamada de `saveOrder` al nombre
    nuevo. NO cambiar la firma de `submitOrder` ni lo que devuelve. NO tocar la logica de
    `lobo_cart` (lineas 55-82) — es otra clave, otro proposito, y funciona.

    NO tocar `app/checkout/page.tsx`. Solo importa `type Order`, que no cambia. Que ese archivo
    quede con diff cero es la mejor prueba de que la refactorizacion no cambio el contrato.
  </action>
  <verify>
    <automated>npm run test:run 2>&amp;1 | tail -20 &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; git diff --exit-code -- app/checkout/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Gate de caracterizacion (Paso 1) y gate RED (Paso 2) ambos registrados en el SUMMARY
    - Gate GREEN: `npm run test:run` en verde
    - `grep -c 'lobo_orders' lib/orders-store.ts` devuelve 0
    - `grep -c 'localStorage' lib/orders-store.ts` devuelve 0
    - `grep -c 'saveOrder' lib/orders-store.ts` devuelve 0 y `grep -rc 'saveOrder' lib/cart-context.tsx` devuelve 0
    - `grep -c 'export function getOrders' lib/orders-store.ts` devuelve 0
    - `grep -c 'export type Order' lib/orders-store.ts` devuelve 1 y `grep -c 'export type OrderStatus' lib/orders-store.ts` devuelve 1
    - `git diff --exit-code -- app/checkout/page.tsx` devuelve 0
    - `grep -c 'lobo_cart' lib/cart-context.tsx` sigue devolviendo el mismo valor que antes del cambio: la persistencia del carrito quedo intacta
    - Existe un test que afirma que `localStorage.setItem` no se llama con `"lobo_orders"`
    - `npx tsc --noEmit` y `npm run lint` pasan
  </acceptance_criteria>
  <done>CLEAN-02 cerrado: cada checkout real deja de escribir una copia muerta, y la confirmacion mas el respaldo de WhatsApp siguen funcionando identico, probado.</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar los botones de pedido de prueba y la llamada muerta a updateOrderStatus</name>
  <read_first>
    - app/admin/page.tsx:1-50 (imports, statusConfig, las constantes MOCK y generateMockOrder)
    - app/admin/page.tsx:63-107 (ValidarTab y handleAddPoints, donde vive la llamada muerta de la linea 95)
    - app/admin/page.tsx:480-560 (los dos botones de pedido de prueba, con sus dos ramas de renderizado)
    - app/admin/page.tsx:288-332 (refresh() y handleStatus — el camino real, contra Supabase, que NO se toca)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md (D-21 — la lista exacta de lo que se elimina)
    - .planning/codebase/CONVENTIONS.md (la alineacion en columnas de los literales de este archivo debe respetarse)
  </read_first>
  <action>
    Eliminar de `app/admin/page.tsx`, exactamente esto y nada mas:

    1. Del import de la linea 4: `updateOrderStatus` y `saveOrder`. Si el import queda vacio,
       borrar la linea entera. El import de tipos de la linea 5 (`Order`, `OrderStatus`) SE QUEDA.
    2. `MOCK_NAMES` (linea 32), `MOCK_ITEMS` (lineas 33-37) y `generateMockOrder()` (lineas 39-49).
    3. El bloque `if (orderId.trim()) { updateOrderStatus(...) }` dentro de `handleAddPoints`
       (lineas 94-96). OJO: el campo de formulario `orderId` y su `useState` SE QUEDAN — se sigue
       usando en la descripcion del historial de puntos (`desc: \`Pedido ${orderId.trim()}\``,
       linea 89) y en el `setOrderId("")` de limpieza (linea 99). Solo se borra la llamada que
       escribia en la clave muerta.
    4. El boton "Agregar pedido de prueba" del estado vacio (lineas 491-497). El bloque vacio
       queda con el icono y el texto "Aun no hay pedidos...". Aprovechar y corregir ese texto:
       hoy dice "Los pedidos enviados por WhatsApp aparecen aqui", lo cual tambien es falso — los
       pedidos aparecen cuando alguien paga en la web. Cambiarlo por algo verdadero y corto.
    5. El boton "+ Agregar pedido de prueba" del pie de la lista (lineas 548-556), incluyendo el
       `{orders.length > 0 && (...)}` que lo envuelve.

    NO tocar: `refresh()`, `handleStatus`, el polling de 10 segundos, `statusConfig`, ni ninguna
    otra parte de `ValidarTab`. El flujo de puntos sigue funcionando exactamente igual — solo deja
    de hacer una escritura que no servia.

    Al borrar dentro de bloques con alineacion en columnas, mantener la alineacion existente en vez
    de reformatear el bloque (CONVENTIONS.md). Un diff que reformatea 40 lineas para borrar 5
    esconde el cambio real.

    Verificar despues que no quedaron imports ni variables sin usar: `npm run lint` con
    `eslint-config-next/typescript` los marca.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build 2>&amp;1 | tail -15 &amp;&amp; npm run test:run 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pedido de prueba' app/admin/page.tsx` devuelve 0
    - `grep -c 'generateMockOrder' app/admin/page.tsx` devuelve 0
    - `grep -c 'MOCK_NAMES\|MOCK_ITEMS' app/admin/page.tsx` devuelve 0
    - `grep -c 'updateOrderStatus' app/admin/page.tsx` devuelve 0
    - `grep -c 'saveOrder' app/admin/page.tsx` devuelve 0
    - `grep -c 'OrderStatus' app/admin/page.tsx` sigue siendo mayor que 0: el tipo se sigue usando
    - `grep -c 'orderId' app/admin/page.tsx` sigue siendo mayor que 0: el campo del formulario no se borro
    - `grep -c 'api/admin/pedidos' app/admin/page.tsx` sigue devolviendo el mismo valor que antes: el camino real contra Supabase quedo intacto
    - `npm run lint` pasa sin warnings de imports o variables sin usar
    - `npm run build` termina sin errores
    - `npm run test:run` sigue en verde
  </acceptance_criteria>
  <done>CLEAN-03 cerrado: el panel ya no ofrece botones que no hacen lo que dicen, y el "marcar entregado" del flujo de puntos dejo de fingir que escribe algo.</done>
</task>

<task type="auto">
  <name>Task 3: Actualizar .context/DEPLOY.md al estado real del sistema</name>
  <read_first>
    - .context/DEPLOY.md (LEER COMPLETO — 33 lineas, se reescribe parcialmente)
    - .planning/codebase/CONCERNS.md (item 12 — por que la nota de Culqi de este doc no es confiable; y la seccion "Resolved (Fase A)" para saber que SI esta resuelto hoy)
    - .env.example (la lista real de variables que el sistema necesita hoy, tras el plan 01-03)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md (D-22)
    - supabase/migrations/20260820000000_pedidos.sql (la tabla que reemplazo al localStorage)
  </read_first>
  <action>
    Actualizar `.context/DEPLOY.md`. Cambios obligatorios:

    **Item 4 del checklist pre-produccion** — hoy dice: "Limitacion conocida: pedidos en
    localStorage — el admin solo ve pedidos del mismo navegador. El respaldo operativo es el boton
    WhatsApp post-pago. Para multi-dispositivo se necesita DB (fase futura)." Es falso desde el
    commit `eb9f243` (2026-08-20). Reemplazarlo por el estado real: los pedidos viven en la tabla
    `pedidos` de Supabase (migracion `20260820000000_pedidos.sql`), el panel los lee de
    `/api/admin/pedidos` desde cualquier dispositivo, la idempotencia esta anclada en
    `culqi_charge_id text not null unique`, y el boton de WhatsApp sigue existiendo como respaldo
    operativo cuando el insert falla despues de un cargo exitoso.

    **Seccion "Estado Culqi (2026-08-12)"** — `CONCERNS.md#12` declara esa nota no confiable. NO
    borrarla: es historia util. Agregarle un aviso al principio diciendo que la informacion es del
    2026-08-12 y que hay que verificarla contra el panel de Culqi antes de confiar en ella, con la
    referencia al plan 01-02 de esta fase (PAY-01), que es quien la verifica en vivo. Si
    `01-CULQI-FLUJO.md` ya existe al momento de ejecutar esta tarea, enlazarlo como la fuente al
    dia.

    **Tabla de env vars** — hoy solo lista las dos de Culqi. Completarla con todo lo que el sistema
    necesita realmente en Vercel, cruzando contra `.env.example`: `SUPABASE_URL`,
    `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `RECLAMOS_EMAIL_TO`, `ADMIN_USER`,
    `ADMIN_PASSWORD`, y las nuevas de esta fase `SENTRY_DSN`, `TELEGRAM_ALERT_BOT_TOKEN`,
    `TELEGRAM_ALERT_CHAT_ID`. Para cada una, una columna que diga que pasa si falta — ese dato es
    el que evita un incidente a las 11 de la noche. Marcar explicitamente cuales son server-only.

    **Nota sobre esta fase** — una seccion corta que diga que la Fase 1 agrega webhook de Culqi,
    rate limiting, cron y monitoreo, con las variables nuevas que eso implica (`CRON_SECRET` llega
    en el plan 01-08; nombrarla como pendiente si todavia no existe).

    NO inventar estado que no se pueda verificar leyendo el repo. Si algo es incierto, escribirlo
    como incierto. Un doc de deploy que afirma cosas que nadie comprobo es peor que uno
    desactualizado, porque el desactualizado al menos tiene fecha.

    Espanol, mismo registro y formato del documento actual.
  </action>
  <verify>
    <automated>grep -c 'localStorage' .context/DEPLOY.md | grep -qv '^0$' &amp;&amp; echo "REVISAR: todavia menciona localStorage, confirmar que es como historia y no como estado vigente"; grep -q 'SUPABASE_SERVICE_ROLE_KEY' .context/DEPLOY.md &amp;&amp; grep -q 'TELEGRAM_ALERT_BOT_TOKEN' .context/DEPLOY.md &amp;&amp; grep -q 'SENTRY_DSN' .context/DEPLOY.md &amp;&amp; grep -q 'pedidos' .context/DEPLOY.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - El item 4 del checklist ya no describe "pedidos en localStorage" como limitacion vigente
    - El documento menciona la tabla `pedidos` de Supabase y la ruta `/api/admin/pedidos`
    - La seccion de estado de Culqi lleva un aviso de fecha y una referencia a PAY-01 / plan 01-02
    - La tabla de env vars lista las 11 variables que el sistema usa hoy, cada una con que pasa si falta
    - `grep -cE 'sk_(live|test)_[A-Za-z0-9]{8}' .context/DEPLOY.md` devuelve 0 — ninguna clave real. Las claves de test que ya estaban en el doc se reemplazan por placeholders
    - Ninguna afirmacion nueva del documento contradice lo que se puede leer en el repo
  </acceptance_criteria>
  <done>CLEAN-04 cerrado: el doc de deploy describe el sistema que existe, no el de hace dos meses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> localStorage | Datos personales de pedidos (nombre, telefono, direccion) persistidos en el dispositivo del cliente |
| repo -> documentacion operativa | `.context/DEPLOY.md` guia decisiones de despliegue; si miente, induce errores en produccion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Information disclosure | `localStorage["lobo_orders"]` con nombre, telefono y direccion de cada pedido en el dispositivo del cliente | mitigate | Este plan elimina la escritura. Nota: las copias YA escritas en dispositivos de clientes existentes no se pueden borrar remotamente; se acepta, el dato es del propio cliente sobre sus propios pedidos y no cruza a otro usuario |
| T-01-17 | Tampering | Refactor de `lib/cart-context.tsx:104`, que corre en cada checkout real, rompe el objeto de confirmacion | mitigate | Caracterizacion obligatoria de la forma del objeto y de `buildWhatsAppUrl` ANTES de quitar la persistencia; criterio de aceptacion de que `app/checkout/page.tsx` queda con diff cero |
| T-01-18 | Repudiation | Borrar la llamada a `updateOrderStatus` sin verificar que era muerta | mitigate | Verificado con grep que escribia en `localStorage["lobo_orders"]`, clave que el panel ya no lee — el estado real vive en `pedidos.estado` via `PATCH /api/admin/pedidos`. `CONCERNS.md` item 7 lo documenta linea por linea |
| T-01-19 | Information disclosure | Claves reales de Culqi filtradas al reescribir `.context/DEPLOY.md` | mitigate | Criterio de aceptacion con grep sobre patrones `sk_live_`/`sk_test_`; las claves de test que ya estaban en el doc se sustituyen por placeholders |
| T-01-20 | Tampering | Borrar de mas en `app/admin/page.tsx` y romper el panel | mitigate | La lista de lo que se elimina esta enumerada linea por linea; criterios de aceptacion que verifican que `orderId`, `OrderStatus` y el camino contra `/api/admin/pedidos` siguen presentes; `npm run build` como gate |
</threat_model>

<verification>
- `npm run test:run` en verde con `__tests__/orders-store.test.ts`
- `npm run build` y `npm run lint` sin errores ni warnings de codigo muerto
- `git diff --exit-code -- app/checkout/page.tsx` en 0
- La clave `lobo_cart` de `lib/cart-context.tsx` intacta
- Ninguna ocurrencia de `lobo_orders` en todo el repo fuera de un comentario historico
- `.context/DEPLOY.md` sin afirmaciones contradichas por el codigo
</verification>

<success_criteria>
1. Un checkout real ya no escribe una copia muerta en localStorage, y la confirmacion mas el link de WhatsApp funcionan identico (CLEAN-02).
2. El panel no tiene botones inertes ni escrituras que fingen cambiar un estado (CLEAN-03).
3. `.context/DEPLOY.md` describe el sistema real, con las 11 env vars y que pasa si falta cada una (CLEAN-04).
4. El carrito sigue sobreviviendo a un refresh: la clave `lobo_cart` no se toco.
</success_criteria>

<output>
Crear `.planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-04-SUMMARY.md` al terminar.
Registrar el nombre final elegido para el reemplazo de `saveOrder` (Claude's Discretion) para que
los planes de Fase 3 y 4 lo conozcan.
</output>
