---
phase: 01-integridad-del-pago-y-red-de-seguridad
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.mts
  - __tests__/menu.test.ts
  - __tests__/helpers/supabase-mock.ts
  - __tests__/api-charge.caracterizacion.test.ts
  - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-HALLAZGOS-CARACTERIZACION.md
autonomous: false
requirements: [INFRA-04]
user_setup: []

must_haves:
  truths:
    - "`npm run test:run` corre en el repo y devuelve exit code 0 con la suite en verde"
    - "La suite falla si alguien hace que `/api/charge` use un precio enviado por el navegador"
    - "El comportamiento actual del handler de cobro queda congelado en tests ANTES de que nadie lo toque"
  artifacts:
    - path: "vitest.config.mts"
      provides: "Config de Vitest con alias @/* y entorno jsdom (extensible a tests de componentes sin rehacerla, D-17)"
      contains: "vite-tsconfig-paths"
    - path: "__tests__/api-charge.caracterizacion.test.ts"
      provides: "Caracterizacion completa de app/api/charge/route.ts tal como esta hoy"
      min_lines: 150
    - path: "__tests__/helpers/supabase-mock.ts"
      provides: "Mock encadenable de getSupabaseAdmin() reusable por el resto de la fase"
    - path: "package.json"
      provides: "Scripts test y test:run"
      contains: "\"test:run\""
  key_links:
    - from: "__tests__/api-charge.caracterizacion.test.ts"
      to: "app/api/charge/route.ts"
      via: "import { POST }"
      pattern: "from \"@/app/api/charge/route\""
    - from: "vitest.config.mts"
      to: "tsconfig.json paths"
      via: "vite-tsconfig-paths plugin"
      pattern: "tsconfigPaths\\(\\)"
---

<objective>
Levantar el runner de tests del repo (hoy no existe ninguno) y congelar en tests el
comportamiento actual de `app/api/charge/route.ts` — el archivo que mueve la plata —
antes de que ninguna otra tarea de la fase lo toque.

Este plan es la precondicion de toda la fase (D-24): sin runner no hay TDD posible.
Y es la red que hace seguro el resto (D-25): las caracterizaciones se escriben contra
el codigo tal como esta hoy, se ven pasar en verde SIN modificar una linea de
produccion, y recien entonces los planes 03, 05 y 06 pueden tocar el handler.

Purpose: INFRA-04 — que la suite falle si alguien vuelve a confiar en el precio que
manda el navegador (el exploit de pagar S/3 un pedido de S/38, arreglado el 2026-08-20,
hoy no tiene regresion que lo proteja).
Output: `vitest.config.mts`, scripts `test`/`test:run`, un mock reusable de Supabase,
la suite de caracterizacion, y un documento de hallazgos si algun test rojo revela un
bug preexistente.

**Punto de despliegue seguro:** este plan NO modifica ni un archivo de produccion.
Solo agrega devDependencies, config y tests. Riesgo de despliegue: cero.
</objective>

<phase_goal>
Objetivo de fase (verbatim de `.planning/ROADMAP.md` §Phase 1):

> Un pago confirmado por Culqi siempre se convierte en un pedido registrado en
> Supabase, con alertas si algo falla, y el servidor resiste intentos de abuso o de
> manipular precio y envio.

Nota para el ejecutor: la linea `**Goal:**` de ROADMAP.md no esta en formato de
historia de usuario. No se inventa una. Se usa el objetivo tal cual esta escrito.
</phase_goal>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md
@.planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@AGENTS.md

<interfaces>
<!-- Contratos que el ejecutor necesita. Extraidos del codigo. NO hace falta explorar. -->

De `lib/menu.ts` (fuente de verdad del precio, importada por cliente y servidor):
- `export type MenuItem = { id: number; category: string; name: string; description: string; price: number; badge: string | null; originalPrice: number | null; image: string | null }`
- `export const MENU_ITEMS: MenuItem[]`
- `export const CATEGORIES: string[]`
- `export function getMenuItem(id: number): MenuItem | undefined`

Precios reales vigentes (soles), utiles para armar asserts deterministas:
id 1 Miami Night = 18 · id 2 Doble Carne = 24 · id 5 Burgazo = 28 · id 10 Gaseosa = 5
· id 11 Limonada = 7 · id 13 Combo Lobo = 25 · id 14 Combo Bestia = 38.
El item mas barato de la carta es la Gaseosa a S/5 = 500 centimos.

De `lib/supabase.ts`:
- `export function getSupabaseAdmin(): SupabaseClient` — singleton perezoso, tira
  `Error("Faltan SUPABASE_URL o SUPABASE_SERVICE_ROLE_KEY")` si faltan env vars.

De `app/api/charge/route.ts` (constantes y contrato observable de HOY):
- `const MIN_CENTS = 300`, `const MAX_CENTS = 50000`, `const MAX_QTY = 20`
- `export async function POST(request: Request): Promise<Response>`
- Body aceptado: `{ tokenId, email, items: [{id, qty}], name, phone, delivery?, address?, lat?, lng? }`
- Respuesta exitosa: `{ chargeId: string, codigo: string, total: number }` (total en soles)

Cadenas de Supabase que el handler usa y que el mock debe soportar:
- `.from("pedidos").insert({...}).select("codigo").single()`
- `.from("pedidos").select("codigo").eq("culqi_charge_id", id).single()`

De `tsconfig.json`: `"paths": { "@/*": ["./*"] }`, `strict: true`, `"**/*.mts"` ya
esta en `include` — no hay que tocar tsconfig.

Doc oficial de Next 16 para Vitest (leida en este repo, no de memoria):
`node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md` — recomienda
`vitest.config.mts` con `plugins: [tsconfigPaths(), react()]` y `test.environment: 'jsdom'`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verificar legitimidad de `vitest` antes de instalar</name>
  <read_first>
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-RESEARCH.md (seccion `## Package Legitimacy Audit`)
  </read_first>
  <what-built>
    Nada todavia. Este es el gate previo a la primera instalacion de paquetes de la fase.
    El audit de RESEARCH.md corrio slopcheck 0.6.1 sobre los 7 paquetes candidatos.
    Seis dieron `OK`. `vitest` dio **`SUS`** con el flag `TYPOSQUAT_RISK`
    ("Suspiciously close to 'vite'"). La research lo clasifico como falso positivo con
    evidencia directa: repo oficial `github.com/vitest-dev/vitest`, creado 2021-12-03
    (~4 anos), y es el runner que la propia documentacion de Next.js que vive en
    `node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md` recomienda.
    Ningun paquete tiene `scripts.postinstall`.
  </what-built>
  <how-to-verify>
    1. Abrir https://www.npmjs.com/package/vitest
    2. Confirmar que el repositorio enlazado es `github.com/vitest-dev/vitest`
    3. Confirmar que el volumen de descargas semanales es de millones, no de decenas
    4. Confirmar que el mantenedor/organizacion es la del ecosistema Vite (`vitest-dev`),
       no una cuenta creada hace semanas
    5. Los otros tres paquetes de esta instalacion (`@vitejs/plugin-react`,
       `vite-tsconfig-paths`, `jsdom`) dieron `OK` en slopcheck y no requieren revision
  </how-to-verify>
  <acceptance_criteria>
    - El humano confirmo explicitamente que `vitest` es el paquete oficial del ecosistema Vite
    - Si el humano NO lo confirma, la tarea 2 no se ejecuta y el plan se detiene
    - Este checkpoint NO es auto-aprobable, ignora `workflow.auto_advance`
  </acceptance_criteria>
  <resume-signal>Escribir "aprobado" o describir el problema encontrado</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Instalar y configurar Vitest con un smoke test que prueba el alias @/*</name>
  <read_first>
    - node_modules/next/dist/docs/01-app/02-guides/testing/vitest.md (guia oficial de Next 16, es la referencia — no usar convenciones de memoria)
    - package.json (scripts y estructura de dependencias actuales)
    - tsconfig.json (confirmar que `paths` y el include de `**/*.mts` ya estan)
    - lib/menu.ts (sujeto del smoke test)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-RESEARCH.md (§Standard Stack — versiones exactas verificadas contra el registro npm el 2026-08-25)
  </read_first>
  <action>
    Instalar como devDependencies exactamente estos cuatro paquetes, con las versiones
    verificadas en RESEARCH.md: `vitest@^4.1.11`, `@vitejs/plugin-react@^6.1.0`,
    `vite-tsconfig-paths@^6.1.1`, `jsdom@^30.0.1`.

    NO instalar `@testing-library/react` ni `@testing-library/dom`. RESEARCH.md los marca
    como instalacion diferida: esta fase no tiene ni un test de componente y no se carga
    una dependencia sin uso. Se suman en Fase 3/4 cuando exista el primer test de componente.

    Crear `vitest.config.mts` en la raiz siguiendo la guia oficial de Next 16:
    `defineConfig` de `vitest/config`, `plugins: [tsconfigPaths(), react()]`,
    `test.environment: "jsdom"`. El entorno jsdom es deliberado y cumple D-17: esta fase
    no necesita DOM, pero deja la config lista para tests de componentes sin rehacerla.
    Agregar `test.include` cubriendo `__tests__/**/*.test.ts` y `__tests__/**/*.test.tsx`,
    y `test.exclude` con `node_modules` y `.next`.

    Agregar a `package.json` los scripts `"test": "vitest"` (modo watch, para desarrollo)
    y `"test:run": "vitest run"` (una pasada, exit code util para CI y para los gates de
    verificacion de esta fase). Todas las tareas de la fase verifican con `test:run`, nunca
    con `test`, para no colgar el ejecutor en modo watch.

    Crear `__tests__/menu.test.ts` como smoke test. Su unico proposito es probar que el
    runner arranca Y que el alias `@/*` resuelve dentro de Vitest — sin
    `vite-tsconfig-paths` un `import { getMenuItem } from "@/lib/menu"` no resuelve y toda
    la fase se cae. Casos: `getMenuItem(1)` devuelve un item con `name` "Miami Night" y
    `price` 18; `getMenuItem(999)` devuelve `undefined`; `MENU_ITEMS` tiene ids unicos
    (protege contra que un futuro duplicado en la carta haga que `getMenuItem` devuelva
    el item equivocado en el recalculo de precio).

    Comentarios en espanol sin tildes, explicando el porque, siguiendo el registro de
    `app/api/charge/route.ts:1-5` (ver CONVENTIONS.md). Sin Prettier en el repo: comillas
    dobles, punto y coma, indentacion de 2 espacios.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -20 &amp;&amp; npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:run` termina con exit code 0 y reporta al menos 3 tests pasando
    - `vitest.config.mts` existe en la raiz y contiene `tsconfigPaths()` en `plugins`
    - `grep -v '^//' package.json | grep -c '"test:run"'` devuelve 1
    - `package.json` NO contiene `@testing-library` en ninguna forma
    - `npx tsc --noEmit` pasa sin errores (strict mode del repo intacto)
    - `npm run lint` pasa sin errores nuevos
    - El smoke test importa via `@/lib/menu`, no por ruta relativa — si el alias se rompiera, el test falla
  </acceptance_criteria>
  <done>El repo tiene runner de tests funcionando, con el alias `@/*` probado, y cero archivos de produccion modificados.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Caracterizar app/api/charge/route.ts — gate VERDE al primer intento</name>
  <read_first>
    - app/api/charge/route.ts (LEER COMPLETO — es el sujeto exacto de la caracterizacion, 161 lineas)
    - lib/menu.ts (precios reales, para asserts deterministas)
    - lib/supabase.ts (forma de `getSupabaseAdmin()` para el mock)
    - lib/culqi.ts:190-214 (como llega el body real desde el navegador)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-CONTEXT.md (D-18 fija la cobertura obligatoria; D-25 fija el gate invertido de esta tarea)
    - .planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-RESEARCH.md (§Pitfall D — fugas de `fetch` mockeado entre tests)
  </read_first>
  <behavior>
    ATENCION — GATE INVERTIDO. Esta NO es una tarea RED-first. Es caracterizacion (D-25).
    La secuencia obligatoria es:
      1. Escribir los tests contra el comportamiento ACTUAL del handler.
      2. Correrlos SIN tocar una sola linea de `app/api/charge/route.ts`.
      3. Gate: TODOS deben pasar en VERDE al primer intento.
      4. Si alguno queda en rojo, eso es un BUG PREEXISTENTE. Se registra como hallazgo
         en `01-HALLAZGOS-CARACTERIZACION.md` y el test se marca con `it.fails(...)` o
         `it.skip(...)` con un comentario que lo enlace al hallazgo. NO se "arregla de
         paso" dentro de esta tarea. Arreglar un bug de produccion en el camino del
         dinero es una decision que se le presenta al humano, no algo que se cuela en un
         commit de tests.

    Cobertura obligatoria (D-18) — cada punto es un `it()` separado:

    Configuracion faltante:
      - Sin `CULQI_SECRET_KEY` en el entorno: responde 500 con `{ error: "Pasarela no configurada" }`.

    Body invalido (antes de tocar Culqi):
      - Body que no es JSON parseable: 400 `{ error: "Solicitud inválida" }`.
      - Falta `tokenId` / `email` / `name` / `phone`, o `items` no es array, o `items` vacio: 400 `{ error: "Datos del pedido inválidos" }`.
      - Una linea con `id` o `qty` no entero (p. ej. `qty: 1.5`): 400 `{ error: "Datos del pedido inválidos" }`.

    Recalculo del precio — EL CORAZON DE INFRA-04:
      - `items: [{id:1,qty:2},{id:10,qty:1}]` produce una llamada a
        `https://api.culqi.com/v2/charges` cuyo body tiene `amount: 4100`
        (2 x 1800 + 500). Assert sobre el argumento real del `fetch` mockeado.
      - **Test antiregresion explicito:** el mismo body pero con un campo extra
        `amount: 300` (o `total: 3`) enviado por el cliente sigue produciendo
        `amount: 4100` en la llamada a Culqi. Este test es el que debe romperse si
        alguien vuelve a confiar en el precio del navegador. Nombrarlo de forma que
        quien lo rompa entienda por que existe.
      - `currency_code` es `"PEN"` y `source_id` es el `tokenId` recibido.

    Bounds:
      - `qty: 21` (MAX_QTY = 20): 400 `{ error: "Cantidad no permitida" }`, sin llamar a Culqi.
      - `qty: 0`: 400 `{ error: "Cantidad no permitida" }`.
      - Total por encima de MAX_CENTS: `[{id:14,qty:20}]` = 76000 centimos: 400
        `{ error: "El monto del pedido no es válido" }`, sin llamar a Culqi.
      - MIN_CENTS (300): documentar en un comentario del test que hoy es INALCANZABLE
        con la carta vigente — el item mas barato es la Gaseosa a 500 centimos. No
        escribir un test que finja cubrirlo. Registrarlo como observacion en el
        documento de hallazgos.
      - `id` inexistente (999): 400 `{ error: "Hay un producto que ya no está disponible" }`.

    Delivery:
      - `delivery: true` con `address` vacio o solo espacios: 400 `{ error: "Falta la dirección de entrega" }`.
      - `delivery: true` con direccion: la fila insertada lleva `direccion` con el string trimmeado.
      - `delivery: false`: la fila insertada lleva `direccion: null`.

    Cargo rechazado por Culqi:
      - `fetch` a Culqi devuelve `ok: false` con `{ user_message: "Tarjeta rechazada" }`:
        402 con ese mensaje, y NO se llama a Supabase.

    Persistencia (las ramas que hoy fallan en silencio):
      - Camino feliz: insert devuelve `{ data: { codigo: "LB-X" }, error: null }` →
        200 con `{ chargeId, codigo: "LB-X", total: 41 }` (total en SOLES, no centimos).
      - **Idempotencia 23505:** insert devuelve `{ data: null, error: { code: "23505" } }`,
        y el select posterior por `culqi_charge_id` devuelve `{ data: { codigo: "LB-PREVIO" } }`
        → 200 con `codigo: "LB-PREVIO"`. Assert de que se consulto por el `culqi_charge_id`
        del cargo, no por otra cosa.
      - **Fallo de insert tras cargo exitoso:** insert devuelve un error con code distinto
        de 23505 → el handler igual responde 200 (el cliente ya pago) y hace
        `console.error` con el texto "Pedido cobrado pero no registrado" y el `cargo.id`.
        Espiar `console.error` con `vi.spyOn`. Este test es el ancla de PAY-05: el plan
        01-03 va a agregar la alerta exactamente aca.
      - **Excepcion tras cargo exitoso:** `getSupabaseAdmin()` tira (env vars faltantes) →
        200 con el codigo generado, y `console.error` con el mismo texto.
  </behavior>
  <action>
    Crear `__tests__/helpers/supabase-mock.ts` con un constructor de mock encadenable de
    `getSupabaseAdmin()`. Tiene que soportar las dos cadenas que el handler usa:
    `.from().insert().select().single()` y `.from().select().eq().single()`. Exponer una
    API que permita fijar por separado el resultado del insert y el del select de
    conflicto, y que registre los argumentos con que fue llamado cada eslabon para poder
    hacer asserts sobre la fila insertada. Este helper lo reusan los planes 01-03, 01-05,
    01-06, 01-07 y 01-08 — disenarlo para eso, no solo para este archivo.

    Crear `__tests__/api-charge.caracterizacion.test.ts`. Primera linea del archivo:
    el docblock `// @vitest-environment node`. Es obligatorio: el entorno por defecto de
    la config es jsdom, y un route handler de Next se prueba contra los `Request`/`Response`
    nativos de Node 20. Dejar un comentario explicando por que esta esa linea, para que
    nadie la borre.

    Mockear `@/lib/supabase` con `vi.mock` apuntando al helper. Mockear `fetch` con
    `vi.stubGlobal("fetch", vi.fn())` en un `beforeEach` y limpiar con
    `vi.unstubAllGlobals()` + `vi.unstubAllEnvs()` + `vi.restoreAllMocks()` en un
    `afterEach` — es el patron que RESEARCH.md §Pitfall D exige para que los tests no se
    contaminen entre si (pasan solos, fallan en suite). Fijar `CULQI_SECRET_KEY` con
    `vi.stubEnv` en el `beforeEach`, y quitarla solo en el test que verifica el 500.

    Armar las `Request` con `new Request("http://localhost/api/charge", { method: "POST",
    headers: { "Content-Type": "application/json" }, body: JSON.stringify(...) })` e
    importar `POST` desde `@/app/api/charge/route`.

    Escribir un helper local `bodyValido(overrides)` que devuelva un body completo valido,
    para que cada test solo exprese su desviacion. Sin eso, 20 tests con 9 campos cada uno
    se vuelven ilegibles y nadie los mantiene.

    Correr `npm run test:run`. Si todo pasa: crear
    `01-HALLAZGOS-CARACTERIZACION.md` con la observacion de MIN_CENTS inalcanzable y una
    linea diciendo que no hubo otros hallazgos. Si algo falla: registrar cada fallo en ese
    documento con el test, el comportamiento esperado, el observado y el impacto, marcar
    el test como `it.fails` con referencia al hallazgo, y NO tocar el codigo de produccion.

    Comentarios en espanol sin tildes.
  </action>
  <verify>
    <automated>npm run test:run 2>&1 | tail -30 &amp;&amp; git diff --exit-code -- app/ lib/ &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - GATE VERDE: `npm run test:run` termina con exit code 0
    - `git diff --exit-code -- app/ lib/` devuelve 0 — cero cambios en produccion en esta tarea
    - La suite contiene al menos un test cuyo nombre menciona explicitamente que el precio del cliente se ignora
    - Existe un test que cubre la rama `23505` y otro que cubre el fallo de insert tras cargo exitoso (D-18)
    - `__tests__/api-charge.caracterizacion.test.ts` empieza con el docblock `// @vitest-environment node`
    - `grep -c 'unstubAllGlobals' __tests__/api-charge.caracterizacion.test.ts` devuelve al menos 1
    - `01-HALLAZGOS-CARACTERIZACION.md` existe, aunque sea para decir que no hubo hallazgos ademas de MIN_CENTS
    - Verificacion antiregresion manual: comentar la linea `const item = getMenuItem(linea.id);` y sustituirla por un precio del body hace fallar la suite. Revertir despues. (Ejecutar y reportar el resultado en el SUMMARY, no dejarlo commiteado.)
  </acceptance_criteria>
  <done>El comportamiento actual de `/api/charge` esta congelado en tests verdes, sin haber modificado produccion. Los planes 03, 05 y 06 ya pueden tocar el handler con red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registro npm → repo | Codigo de terceros entra al proyecto al instalar devDependencies |
| suite de tests → produccion | Un test mal escrito puede dar falsa confianza sobre el camino del dinero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | `npm install` de vitest/@vitejs-plugin-react/vite-tsconfig-paths/jsdom | mitigate | Audit de slopcheck ya corrido en RESEARCH.md; `vitest` marcado `SUS` pasa por `checkpoint:human-verify` bloqueante (Task 1); verificado que ninguno tiene `scripts.postinstall`; versiones fijadas con `^` sobre las verificadas contra el registro npm el 2026-08-25 |
| T-01-01 | Tampering | `__tests__/api-charge.caracterizacion.test.ts` | mitigate | El test antiregresion de precio se valida activamente: se rompe el codigo a proposito y se confirma que la suite falla. Un test que no puede fallar no protege nada |
| T-01-02 | Repudiation | Caracterizacion que oculta un bug preexistente | mitigate | D-25: un test rojo antes de mover una linea se registra como hallazgo en `01-HALLAZGOS-CARACTERIZACION.md` y se marca `it.fails`, nunca se "arregla de paso" |
| T-01-03 | Information disclosure | Credenciales reales filtradas en fixtures de test | mitigate | Todas las env vars de los tests se inyectan con `vi.stubEnv` usando valores obviamente falsos (`sk_test_x`); prohibido leer `.env.local` desde un test |
| T-01-04 | Denial of Service | `npm run test` en modo watch cuelga la ejecucion automatizada | mitigate | Todos los gates de la fase usan `npm run test:run` (una pasada); `test` en watch queda solo para uso humano |
</threat_model>

<verification>
- `npm run test:run` en verde, exit code 0
- `npx tsc --noEmit` sin errores
- `npm run lint` sin errores nuevos
- `git diff --stat` no muestra ningun archivo bajo `app/` ni `lib/`
- La caracterizacion cubre los cuatro puntos obligatorios de D-18: recalculo, bounds, rama `23505`, fallo de Supabase tras cargo exitoso
</verification>

<success_criteria>
1. El repo tiene runner de tests donde antes no habia ninguno (INFRA-04).
2. La suite falla si alguien hace que `/api/charge` use el precio del navegador — probado rompiendo el codigo a proposito.
3. Cero archivos de produccion modificados: el despliegue de este plan no puede romper el checkout.
4. Los planes 01-03, 01-05 y 01-06 quedan habilitados para tocar `app/api/charge/route.ts` con red debajo.
</success_criteria>

<output>
Crear `.planning/phases/01-integridad-del-pago-y-red-de-seguridad/01-01-SUMMARY.md` al terminar.
Incluir obligatoriamente: el resultado de la verificacion antiregresion (romper el codigo
a proposito y confirmar que la suite falla), y la lista de hallazgos de caracterizacion
si los hubo.
</output>
