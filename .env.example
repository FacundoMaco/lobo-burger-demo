# Culqi — https://culqi.com (panel: https://integ-panel.culqi.com en test)
# La llave pública se expone al navegador; la secreta SOLO vive en el servidor.
NEXT_PUBLIC_CULQI_PUBLIC_KEY=pk_test_xxxxxxxxxxxx
CULQI_SECRET_KEY=sk_test_xxxxxxxxxxxx

# Supabase — libro de reclamaciones. Ambas son server-only:
# el formulario escribe a través de app/api/reclamaciones, nunca desde el navegador.
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJxxxxxxxxxxxx

# Resend — aviso por email a Jaime en cada reclamo nuevo.
# Si quedan vacías, el reclamo igual se registra (solo no se envía el aviso).
RESEND_API_KEY=re_xxxxxxxxxxxx
RECLAMOS_EMAIL_TO=correo-del-negocio@ejemplo.com

# Panel de pedidos — Basic Auth. Sin estas dos, /admin devuelve 503.
ADMIN_USER=usuario
ADMIN_PASSWORD=una-clave-larga

# Sentry — monitoreo de errores server-side (instrumentation.ts).
# Vacía: la app funciona igual, sin captura de errores.
SENTRY_DSN=https://xxxxxxxxxxxx@xxxxxxxxxxxx.ingest.sentry.io/xxxxxxxxxxxx

# Telegram — canal de alertas técnicas (lib/alertas.ts). Server-only: NUNCA
# deben llevar el prefijo NEXT_PUBLIC_. Vacías: la alerta cae a console.error
# en los logs de Vercel, nadie se entera hasta revisarlos a mano.
TELEGRAM_ALERT_BOT_TOKEN=xxxxxxxxxx:xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_ALERT_CHAT_ID=-1000000000000

# Cron de reconciliación (vercel.json + app/api/cron/reconciliacion) — Vercel
# manda este valor automáticamente como header "Authorization: Bearer <valor>"
# en cada invocación programada. Generar con `openssl rand -hex 32`. Sin esta
# variable la ruta responde 401 a todo, incluido al propio Vercel: falla
# cerrada, nunca abierta por omisión.
CRON_SECRET=un-secreto-largo-generado-con-openssl
